---
phase: 01-route-groups
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/(fa)/layout.tsx
  - src/app/(fa)/fa/page.tsx
  - scripts/checks/route-smoke.mjs
  - .planning/phases/01-route-groups/artifacts/routes-after-fa.txt
  - .planning/phases/01-route-groups/01-VALIDATION.md
autonomous: true
requirements: [ROUTE-02]

must_haves:
  truths:
    - "GET /fa returns 200 and its root element is exactly <html lang=\"fa\" dir=\"rtl\">."
    - "GET / still returns <html lang=\"en\"> with no dir attribute, after the second root layout exists."
    - "Every route in the 01-01 smoke baseline still returns the same status, redirect target, <html> tag, and chrome markers."
    - "An unmatched URL still returns HTTP 404 with the branded body marker and lang=\"en\" — verified after the second root layout exists, because that is the moment the composition can break."
    - "The sorted manifest values gain exactly one entry, /fa, and lose none."
    - "npx tsc --noEmit exits 0 and npx next build exits 0 on a cold cache."
    - "src/middleware.ts, ConditionalChrome.tsx, next.config.ts, package.json, and both lockfiles still match the frozen-files.sha fingerprint 01-01 captured — in particular next.config.ts is untouched, so no OQ-1 contingency was silently applied."
  artifacts:
    - src/app/(fa)/layout.tsx
    - src/app/(fa)/fa/page.tsx
    - .planning/phases/01-route-groups/artifacts/routes-after-fa.txt
  key_links:
    - "src/app/(fa)/layout.tsx imports @/styles/globals.css — Tailwind is loaded per root layout, not globally, so without it every /fa page renders completely unstyled."
    - "src/app/(fa)/layout.tsx sets metadataBase — without it, relative OG image URLs on /fa resolve against localhost at build time."
    - "The Farsi page lives at src/app/(fa)/fa/page.tsx, not src/app/(fa)/page.tsx — the literal fa directory is what emits the /fa URL; without it the route resolves to / and collides with the English home page."
    - "Neither (en) nor (fa) may have a layout above it — a surviving src/app/layout.tsx would nest one html document inside the other."
---

<objective>
Add the second root layout. Create `src/app/(fa)/layout.tsx` emitting `<html lang="fa" dir="rtl">` and one placeholder Farsi route at `src/app/(fa)/fa/page.tsx`, then re-run the entire 01-01 gate — because adding a second root layout is the exact moment the global 404 composition and cross-tree behavior can change.

Purpose: this is the foundation the whole milestone stands on. Only a root layout can emit `<html lang dir>`, and only route groups let two root layouts coexist without pushing a locale segment onto 151 guide routes, the dashboard, and every API route.

Output: two new files totalling well under 40 lines, a `--fa-check` mode on the existing route smoke tool, and a re-proof that the English tree is untouched.

Requirement: ROUTE-02 (existing English routes keep their exact URLs and rendering behavior — re-proven under two root layouts). This plan also lays the document-level `lang="fa" dir="rtl"` foundation that ROUTE-01 completes in Phase 4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-route-groups/01-RESEARCH.md
@.planning/phases/01-route-groups/01-PATTERNS.md
@.planning/phases/01-route-groups/01-VALIDATION.md
@.planning/phases/01-route-groups/01-01-SUMMARY.md
</context>

<hard_constraints>
1. **NEVER run `npm run build`.** It chains `prisma migrate deploy` against the PRODUCTION database. The build gate is `npx next build`.
2. **No new dependencies.** `package.json` and both lockfiles end this plan byte-identical.
3. **No push, no merge, no deploy.** Local commits only.
4. **Do not edit `next.config.ts`.** In particular, do NOT enable `experimental.globalNotFound` and do NOT author `src/app/global-not-found.tsx`. That contingency is not pre-authorized; see the OQ-1 stop rule in Task 3.
5. **Do not touch** `src/middleware.ts`, `src/components/appSkeleton/ConditionalChrome.tsx`, or anything under `src/app/(en)/`. This plan is additive only.
6. **Do not duplicate** `sitemap.ts`, `robots.ts`, or `icon.svg` into `(fa)`. Two routes resolving to one URL is a build-time Conflicting paths error, and Phase 5 extends the single `sitemap.ts` with Farsi URLs.
7. **Quote every path containing `(fa)` or `(en)`.** Parentheses are grouping operators in bash and PowerShell.
8. **No `Co-Authored-By:` line** in any commit message.
</hard_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Author the (fa) root layout and the placeholder Farsi route</name>

  <files>
    src/app/(fa)/layout.tsx (new),
    src/app/(fa)/fa/page.tsx (new)
  </files>

  <read_first>
    - `.planning/phases/01-route-groups/01-PATTERNS.md` — the `src/app/(fa)/layout.tsx` section (which three things to copy from the English layout and the explicit do-not-copy list) and the `src/app/(fa)/fa/page.tsx` section (the `privacy/page.tsx` analog).
    - `src/app/(en)/layout.tsx` — the source of the three copied elements: the stylesheet import, `metadataBase`, and the `<body>` colour-token classes.
    - `src/app/(en)/privacy/page.tsx` — the shape to mirror for the placeholder page: no `"use client"`, a plain object literal `metadata` export (this repo does not import the `Metadata` type in page files), a single `<main>` with `max-w-4xl mx-auto px-6 py-16`, colours only via CSS variables.
    - `.planning/phases/01-route-groups/01-RESEARCH.md` — "Pattern 1: Two root layouts via route groups" and "Pattern 2: Minimal Phase-1 (fa) root layout".
  </read_first>

  <action>
Create exactly two files. Add nothing else under `src/app/(fa)/`.

**File 1 — `src/app/(fa)/layout.tsx`.** This is a root layout: it renders its own complete html document. It is deliberately the smallest layout that satisfies the phase, because Phase 4 owns every piece of Farsi chrome.

Contents, in order:
- One import and one only: the side-effect stylesheet import of `@/styles/globals.css` in alias form. This is load-bearing and non-obvious: Tailwind is loaded per root layout, not globally, so omitting it renders every Farsi page completely unstyled. Use the alias form, never a relative path.
- An exported `metadata` object literal with two keys: `metadataBase` set to a `new URL` of the production origin `https://www.neuronomixer.com`, and a plain string `title` of `NeuroNomixer`. `metadataBase` is also load-bearing and non-obvious: without it, relative OG image URLs on `/fa` resolve against localhost at build time. Do not copy `title.template`, `openGraph`, `twitter`, or the `google-site-verification` entry from the English layout.
- A default-exported function named `FaRootLayout` taking `{ children }` typed as `{ children: React.ReactNode }`, returning an `html` element with `lang` set to `fa` and `dir` set to `rtl`, containing a `body` whose `className` is the string `bg-[var(--background)] text-[var(--color-text)]`, containing `{children}`.

Do NOT add, and do NOT reference in a comment: the `Inter` font or the `--font-sans` variable, the two Google Analytics `Script` blocks, `NextAuthProvider`, `ConditionalChrome`, `GeneralSignupPrompt`, `FlushPendingCompletions`, `GoogleAnalyticsTracker`, `SpeedInsights`, or the `transition-colors duration-300` classes. All of those belong to Phase 4, and one acceptance criterion counts the import lines in this file at exactly 1.

Add no comments. The two non-obvious WHYs above are recorded here in the plan and in PATTERNS.md, which is where they belong; the file itself is three lines of JSX and the global rule is comments only where the WHY is non-obvious in the code's own context.

**File 2 — `src/app/(fa)/fa/page.tsx`.** The directory nesting is exact and is not a typo: the group folder `(fa)` is stripped from the URL, so the literal `fa` directory inside it is what produces the `/fa` URL. A page at `src/app/(fa)/page.tsx` would resolve to `/` and collide with the English home page, which is a build-time Conflicting paths error.

Mirror `privacy/page.tsx`: an exported plain-object `metadata` with a Farsi `title` and `description`, and a default-exported server component (no `"use client"`) rendering a single `main` with `max-w-4xl mx-auto px-6 py-16 text-[var(--color-text)]`, an `h1` with `text-4xl font-bold mb-8 text-[var(--color-accent)]`, and one short Farsi paragraph. Content is a placeholder: a Farsi heading naming the site and one sentence saying the Persian edition is on its way. Keep it to a heading and a sentence.

Do not add `alternates.canonical` — hreflang and canonicals are Phase 5. Do not hard-code any colour; use the CSS variable tokens only. The em-dash prohibition does not apply to Farsi output, but there is no reason to use one here either.

After writing both files, run `npx tsc --noEmit` and confirm it exits 0. Do not run a build yet; Task 3 owns the full gate.

Commit with subject `feat(01-02): add the (fa) root layout and placeholder Farsi route`. No `Co-Authored-By` line.
  </action>

  <verify>
    <automated>npx tsc --noEmit &amp;&amp; test "$(grep -c '^import ' 'src/app/(fa)/layout.tsx')" = "1" &amp;&amp; test "$(grep -c '@/styles/globals.css' 'src/app/(fa)/layout.tsx')" = "1" &amp;&amp; test "$(grep -c 'lang="fa"' 'src/app/(fa)/layout.tsx')" = "1" &amp;&amp; test "$(grep -c 'dir="rtl"' 'src/app/(fa)/layout.tsx')" = "1" &amp;&amp; test "$(grep -c 'metadataBase' 'src/app/(fa)/layout.tsx')" = "1" &amp;&amp; test -f 'src/app/(fa)/fa/page.tsx' &amp;&amp; test ! -f 'src/app/(fa)/page.tsx' &amp;&amp; test "$(grep -c 'use client' 'src/app/(fa)/fa/page.tsx')" = "0" &amp;&amp; echo "FA SCAFFOLD OK"</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `src/app/(fa)/layout.tsx` exists; `grep -c '^import ' "src/app/(fa)/layout.tsx"` is exactly `1`, and `grep -c '@/styles/globals.css' "src/app/(fa)/layout.tsx"` is `1`.
    - `grep -c 'lang="fa"' "src/app/(fa)/layout.tsx"` is `1` and `grep -c 'dir="rtl"' "src/app/(fa)/layout.tsx"` is `1`.
    - `grep -c 'metadataBase' "src/app/(fa)/layout.tsx"` is `1`.
    - `wc -l "src/app/(fa)/layout.tsx"` is at most `20`.
    - `src/app/(fa)/fa/page.tsx` exists and `src/app/(fa)/page.tsx` does NOT exist.
    - `grep -c 'use client' "src/app/(fa)/fa/page.tsx"` is `0`.
    - `git status --porcelain "src/app/(en)"` prints nothing (this plan is additive; the English tree is untouched).
    - `git hash-object src/middleware.ts src/components/appSkeleton/ConditionalChrome.tsx next.config.ts package.json package-lock.json pnpm-lock.yaml | diff - .planning/phases/01-route-groups/artifacts/frozen-files.sha` produces no output and exits 0. Use this fingerprint, not `git diff HEAD`: the working tree carries pre-existing uncommitted lockfile drift from before the phase, which 01-01 froze into this file.
    - `git log -1 --pretty=%s` is `feat(01-02): add the (fa) root layout and placeholder Farsi route`, and `git log -1 --pretty=%B | grep -ci 'co-authored-by'` is `0`.
  </acceptance_criteria>

  <done>
    Two files exist under `src/app/(fa)/`; the layout is a root layout emitting a Farsi RTL document with exactly one import; the placeholder page sits at the nesting that emits `/fa`; the type check is clean and the change is committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend the route smoke tool with a --fa-check mode</name>

  <files>
    scripts/checks/route-smoke.mjs
  </files>

  <read_first>
    - `scripts/checks/route-smoke.mjs` — read the whole file; you are adding a third mode alongside `--record` and `--verify` and reusing its existing `probe` function and `BASE` constant.
    - `.planning/phases/01-route-groups/01-VALIDATION.md` — the two Phase SC-3 rows in the requirements-to-test map that this mode discharges.
  </read_first>

  <action>
Add a third mode to `scripts/checks/route-smoke.mjs`. Reuse the existing `probe` function, the existing `BASE` constant, and the existing `node:assert` style with a message string on every assertion. Do not change the behavior of `--record` or `--verify`, and do not change the shape of the recorded baseline object — 01-01's committed baseline must stay readable.

`--fa-check` mode:
- Probe `/fa`. Assert `status` is 200. Assert its `htmlTag` is exactly the opening html tag carrying `lang="fa"` and `dir="rtl"` and no other attributes. Compare against the exact expected string rather than testing for substrings, so a stray extra attribute is caught.
- Probe `/`. Assert its `htmlTag` is exactly the opening html tag carrying `lang="en"` and nothing else — in particular it must carry no direction attribute. This is the guard against the Farsi layout leaking document-level RTL onto the English tree.
- Probe `/blog`. Assert the same exact English html tag as `/`.
- Probe `/__unmatched-url-smoke-check`. Assert `status` is 404, `hasBrandedNotFound` is true, and its `htmlTag` carries `lang="en"`. This repeats the assertion `--verify` already makes, deliberately: it is the single highest-risk unknown in the phase and it is cheap to assert twice.
- On success print `route-smoke.mjs: FA CHECK ALL PASS`. On failure let the assertion throw, which exits non-zero, and make the assertion message name the route and the expected-versus-actual html tag so the failure is diagnosable without a rerun.

Update the top-of-file docblock to list the new mode's literal run command alongside the existing two, and update the usage output printed when no recognized flag is passed.

Do not run this yet against a server — Task 3 owns the gated run. Verify only that the file still parses and the type check is clean.

Commit with subject `chore(01-02): add --fa-check mode to the route smoke tool`. No `Co-Authored-By` line.
  </action>

  <verify>
    <automated>node --check scripts/checks/route-smoke.mjs &amp;&amp; npx tsc --noEmit &amp;&amp; test "$(grep -c -e '--fa-check' scripts/checks/route-smoke.mjs)" -ge 2 &amp;&amp; test "$(grep -c -e '--record' scripts/checks/route-smoke.mjs)" -ge 2 &amp;&amp; test "$(grep -c -e '--verify' scripts/checks/route-smoke.mjs)" -ge 2 &amp;&amp; echo "FA CHECK MODE WIRED"</automated>
  </verify>

  <acceptance_criteria>
    - `node --check scripts/checks/route-smoke.mjs` exits 0 (the file still parses as ESM).
    - `npx tsc --noEmit` exits 0.
    - `grep -c -e '--fa-check' scripts/checks/route-smoke.mjs` is at least `2` (the docblock mention and the flag test).
    - `grep -c -e '--record' scripts/checks/route-smoke.mjs` and `grep -c -e '--verify' scripts/checks/route-smoke.mjs` are each still at least `2` (the pre-existing modes were not removed).
    - `node scripts/checks/route-smoke.mjs` with no flag exits 1 and its usage output names all three modes.
    - `node -e "const b=require('./.planning/phases/01-route-groups/artifacts/route-smoke.baseline.json'); if(!Array.isArray(b.routes)) throw new Error('baseline shape broken')"` exits 0 (the 01-01 baseline is still readable).
    - `git diff HEAD~1 --stat -- "src/app"` prints nothing (this task changes no application code).
    - `git log -1 --pretty=%s` is `chore(01-02): add --fa-check mode to the route smoke tool`, and `git log -1 --pretty=%B | grep -ci 'co-authored-by'` is `0`.
  </acceptance_criteria>

  <done>
    `scripts/checks/route-smoke.mjs` has three working modes; `--record` and `--verify` are behaviorally unchanged and the 01-01 baseline still parses; the new mode is committed and ready for the Task 3 gated run.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-run the entire 01-01 gate under two root layouts, with a blocking 404 check</name>

  <files>
    .planning/phases/01-route-groups/artifacts/routes-after-fa.txt (new),
    .planning/phases/01-route-groups/01-VALIDATION.md
  </files>

  <read_first>
    - `.planning/phases/01-route-groups/01-RESEARCH.md` — Pitfall 6 ("The global 404 loses its home when a second root layout appears"), Pitfall 7 (cross-tree navigation is a full page load), Open Question OQ-1, and the "Sampling Rate" note that passing in 01-01 proves nothing about 01-02.
    - `.planning/phases/01-route-groups/01-VALIDATION.md` — the Manual-Only Verifications table, including the OQ-1 stop rule.
    - `.planning/phases/01-route-groups/artifacts/routes-before.txt` — the 284-line ground truth this task diffs against.
    - `.planning/phases/01-route-groups/01-01-SUMMARY.md` — the recorded 01-01 gate results you are re-proving.
  </read_first>

  <action>
Adding `src/app/(fa)/layout.tsx` created the second root layout. That is the exact moment the global 404 composition, cross-tree navigation, and document-level attributes can change, so the entire 01-01 gate re-runs here from cold. A pass in 01-01 proves nothing about this state.

**Part A — the cold gate.** From the repo root, in this order: remove `.next` and `tsconfig.tsbuildinfo`; run `npx tsc --noEmit` (must exit 0); run `npx next build` (must exit 0). Never `npm run build`. If the build reports that `@prisma/client` is not initialized, run `npx prisma generate` (contacts no database) and retry once.

**Part B — the URL-delta proof.** Extract `Object.values(...)` from `./.next/app-path-routes-manifest.json`, sort, join on newlines, and write to `.planning/phases/01-route-groups/artifacts/routes-after-fa.txt`. Diff it against `routes-before.txt`. Unlike 01-01, this diff is not empty: it must show exactly one added line, `/fa`, and zero removed lines. Concretely, the diff lines beginning with the removal marker must number zero, and the diff lines beginning with the addition marker must be exactly one and must be `/fa`. A removal here means a route was lost to the second root layout, most likely `/_not-found`; if that happens, STOP.

**Part C — the English regression proof.** Start `npx next start` in the background, poll `/robots.txt` until it responds or 90 seconds elapse (if port 3000 is occupied, use `-p 3100` and set `GATE_BASE_URL` accordingly), then run `node scripts/checks/route-smoke.mjs --verify`. It re-fetches exactly the URL set 01-01 recorded and diffs every field, so this proves the English tree is byte-behavior-identical with the Farsi root layout present. Must print `ALL PASS` and exit 0.

**Part D — the Farsi proof, and the blocking OQ-1 check.** Against the same running server, run `node scripts/checks/route-smoke.mjs --fa-check`. This asserts `/fa` serves exactly `<html lang="fa" dir="rtl">` at status 200, that `/` and `/blog` still serve exactly `<html lang="en">` with no direction attribute, and that an unmatched URL still returns 404 with the branded body and `lang="en"`.

**STOP RULE (blocking, not negotiable).** Next.js documents that with multiple root layouts there is no single layout from which to compose a global 404, and it does not document what actually renders in that situation. If the unmatched-URL assertion fails — any status other than 404, a missing branded marker, an unstyled default Next 404 page, or an html tag carrying `dir="rtl"` — then:
- STOP execution immediately. Do not proceed to Part E or F.
- Do NOT edit `next.config.ts`. Do NOT enable `experimental.globalNotFound`. Do NOT author `src/app/global-not-found.tsx`. Do NOT add a `not-found.tsx` inside `(fa)` to "balance" it. None of these is pre-authorized; the experimental flag needs Saeid's explicit sign-off and it also bypasses the layout entirely, so adopting it unprovoked would itself change 404 rendering.
- Report to Saeid with: the exact observed status, the exact observed html tag, the first 40 lines of the response body, and the two options on the table — (a) accept Next's default 404 for unmatched URLs, or (b) enable `experimental.globalNotFound` and author a complete `global-not-found.tsx` document. Both are decisions, not fixes.

Stop the background server once Parts C and D pass.

**Part E — the untouched-file and guide-discovery proofs.** `node scripts/mobile-gate.mjs --list-only` must print exactly 151 lines (no server needed). Then re-run the six-path `git hash-object` fingerprint that 01-01 captured and pipe it into `diff - .planning/phases/01-route-groups/artifacts/frozen-files.sha`; it must produce no output. This is the check that proves `next.config.ts` was not touched, which matters most here because the OQ-1 contingency is a `next.config.ts` flag. Do not substitute a `git diff HEAD` check: the working tree carries pre-existing uncommitted lockfile drift from before the phase, and a HEAD-relative check would report a false failure.

**Part F — evidence and the validation map.** In `.planning/phases/01-route-groups/01-VALIDATION.md`, flip the Status cell for rows `01-02-T1`, `01-02-T2`, and `01-02-T3` from pending to green, and set the frontmatter `status` to `complete`. Do not restructure the table. Then commit `routes-after-fa.txt` plus the updated `01-VALIDATION.md` with subject `docs(01-02): record post-(fa) route manifest evidence`. No `Co-Authored-By` line.
  </action>

  <verify>
    <automated>rm -rf .next tsconfig.tsbuildinfo &amp;&amp; npx tsc --noEmit &amp;&amp; npx next build &amp;&amp; node -e "console.log(Object.values(require('./.next/app-path-routes-manifest.json')).sort().join('\n'))" > .planning/phases/01-route-groups/artifacts/routes-after-fa.txt &amp;&amp; test "$(diff .planning/phases/01-route-groups/artifacts/routes-before.txt .planning/phases/01-route-groups/artifacts/routes-after-fa.txt | grep -c '^&lt;')" = "0" &amp;&amp; test "$(diff .planning/phases/01-route-groups/artifacts/routes-before.txt .planning/phases/01-route-groups/artifacts/routes-after-fa.txt | grep '^&gt;' | tr -d '&gt; ')" = "/fa" &amp;&amp; test "$(node scripts/mobile-gate.mjs --list-only | wc -l | tr -d ' ')" = "151" &amp;&amp; git hash-object src/middleware.ts src/components/appSkeleton/ConditionalChrome.tsx next.config.ts package.json package-lock.json pnpm-lock.yaml | diff - .planning/phases/01-route-groups/artifacts/frozen-files.sha &amp;&amp; echo "ROUTE-02 HELD: 0 URLs lost, exactly /fa added, frozen files intact"</automated>
    <automated>node scripts/checks/route-smoke.mjs --verify &amp;&amp; node scripts/checks/route-smoke.mjs --fa-check</automated>
    <human-check>
      In Saeid's Chrome against a local `npx next start`: (1) load `/fa` and confirm the page is styled, not raw unstyled HTML, and that text flows right to left; (2) open devtools and confirm the root element reads `lang="fa" dir="rtl"`; (3) load `/` and confirm it reads `lang="en"` with no `dir` attribute and that the navbar and footer are present; (4) request a nonsense URL such as `/definitely-not-a-real-page` and confirm the branded 404 page renders, left to right, with its two call-to-action buttons. Item 4 is the OQ-1 human confirmation of the automated assertion; if it disagrees with the automated result, trust the browser and report.
    </human-check>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 on a cold cache and `npx next build` exits 0. `npm run build` was never invoked.
    - `wc -l .planning/phases/01-route-groups/artifacts/routes-after-fa.txt` prints `285`.
    - The diff of `routes-before.txt` against `routes-after-fa.txt` has zero removal lines and exactly one addition line, whose value is `/fa`.
    - `node scripts/checks/route-smoke.mjs --verify` exits 0 and its final line is `route-smoke.mjs: ALL PASS`.
    - `node scripts/checks/route-smoke.mjs --fa-check` exits 0 and its final line is `route-smoke.mjs: FA CHECK ALL PASS`.
    - The `/fa` response html tag is exactly `<html lang="fa" dir="rtl">`.
    - The `/` and `/blog` response html tags are each exactly `<html lang="en">`.
    - An unmatched URL returns status `404` with a body containing `Page not found` and an html tag containing `lang="en"`. If this fails, the stop rule fired and no contingency was applied.
    - `node scripts/mobile-gate.mjs --list-only | wc -l` prints `151`.
    - `git hash-object src/middleware.ts src/components/appSkeleton/ConditionalChrome.tsx next.config.ts package.json package-lock.json pnpm-lock.yaml | diff - .planning/phases/01-route-groups/artifacts/frozen-files.sha` produces no output and exits 0 (in particular, `next.config.ts` is unchanged, so no OQ-1 contingency was applied).
    - `git log --oneline -3 --pretty=%s` shows the evidence subject, the `--fa-check` chore subject, and the `(fa)` scaffold feat subject, in that order.
    - `git log -3 --pretty=%B | grep -ci 'co-authored-by'` is `0`.
    - `.planning/phases/01-route-groups/01-VALIDATION.md` has `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`, and all six Per-Task Verification Map rows show a green status.
  </acceptance_criteria>

  <done>
    A cold `tsc` and `npx next build` both exit 0 with two root layouts present; the manifest gained exactly `/fa` and lost nothing; the English smoke baseline verifies clean; `/fa` serves `<html lang="fa" dir="rtl">` while `/` still serves `<html lang="en">`; the branded 404 survived, or the stop rule fired and the decision is with Saeid; the mobile gate still discovers 151 guides; the validation map is complete.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
Created by this plan:
- `src/app/(fa)/` — new route-group directory (URL-invisible), the second root-layout group
- `src/app/(fa)/layout.tsx` — new root layout; default export `FaRootLayout`; exports `metadata` with `metadataBase` and `title`; emits `<html lang="fa" dir="rtl">`; single import of `@/styles/globals.css`
- `src/app/(fa)/fa/page.tsx` — new placeholder page; default-exported server component; exports `metadata`; emits the `/fa` URL
- `.planning/phases/01-route-groups/artifacts/routes-after-fa.txt` — 285 sorted URL values (284 pre-existing plus `/fa`)

Modified by this plan:
- `scripts/checks/route-smoke.mjs` — gains a third mode, `--fa-check`; `--record` and `--verify` behavior and the baseline JSON shape are unchanged
- `.planning/phases/01-route-groups/01-VALIDATION.md` — Per-Task Verification Map completed, frontmatter marked complete

URLs added: exactly one, `/fa`.
URLs removed or changed: none.
Import specifiers changed: none. This plan is purely additive to the application tree.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous internet → `/fa` | A brand-new public route with a brand-new root layout; it must not inherit or expose anything the English layout provides |
| Anonymous internet → unmatched URLs | With two root layouts the router has no single layout from which to compose a global 404; this is the one capability whose owning tier genuinely changes in this phase |
| Anonymous internet → `/dashboard/*` | Unchanged auth gating; re-proven because a second root layout is a structural change to the app tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Denial of Service | global 404 composition | high | mitigate | The branded 404 may silently degrade to Next's default unstyled page once a second root layout exists, or render right-to-left. Mitigated by a blocking assertion in `--fa-check` plus the repeat assertion in `--verify`, plus a browser human check. On failure the executor STOPS; no contingency is pre-authorized. |
| T-01-09 | Denial of Service | `src/app/(fa)/layout.tsx` | medium | mitigate | Omitting the `@/styles/globals.css` import renders every Farsi page unstyled, and Tailwind is loaded per root layout so nothing else would supply it. Gated by an exact grep count on the import plus the browser human check that `/fa` is styled. |
| T-01-10 | Information Disclosure | `src/app/(fa)/layout.tsx` | low | mitigate | Copying `NextAuthProvider`, the analytics `Script` blocks, or `SpeedInsights` into the Farsi layout would extend session and telemetry surface to a tree that has no authenticated users and no consent story. Gated by asserting the file has exactly one import line and is at most 20 lines. |
| T-01-11 | Tampering | `next.config.ts` | high | mitigate | The documented OQ-1 contingency is an experimental framework flag. Enabling it without Saeid's sign-off would change production 404 rendering and violate the standing constraint on unapproved configuration changes. Gated by hard constraint 4, the explicit stop rule in Task 3, and the `frozen-files.sha` fingerprint check, which fails if `next.config.ts` changed by even one byte. |
| T-01-12 | Elevation of Privilege | `src/middleware.ts` | medium | accept | The matcher is `/dashboard/:path*`, a URL pattern, and `/fa` does not intersect it. No change is needed and none is made. Accepted with verification: the `frozen-files.sha` fingerprint plus the `/dashboard` redirect probe in the smoke baseline. |
| T-01-SC | Tampering | package manager | high | mitigate | This plan installs nothing. No package-manager task exists; gated by the `frozen-files.sha` fingerprint over `package.json` and both lockfiles. Any proposed install is escalated to Saeid, never executed. |
| T-01-DB | Tampering | production Postgres | high | mitigate | `npm run build` chains `prisma migrate deploy` against production. Forbidden by hard constraint 1; the gate is `npx next build`. `npx prisma generate` is the only permitted Prisma command. |
</threat_model>

<verification>
1. Cold `npx tsc --noEmit` exits 0 with two root layouts present.
2. Cold `npx next build` exits 0.
3. `routes-after-fa.txt` versus `routes-before.txt`: zero removals, exactly one addition, `/fa`.
4. `node scripts/checks/route-smoke.mjs --verify` prints `ALL PASS` — the English tree is unchanged.
5. `node scripts/checks/route-smoke.mjs --fa-check` prints `FA CHECK ALL PASS` — `/fa` is Farsi RTL, `/` and `/blog` are English LTR, and the branded 404 survived.
6. `node scripts/mobile-gate.mjs --list-only | wc -l` prints 151.
7. The six-path `git hash-object` fingerprint still matches `frozen-files.sha` — `next.config.ts` in particular is untouched.
8. Human check in Saeid's Chrome: `/fa` styled and right-to-left, `/` unchanged, nonsense URL shows the branded 404.
</verification>

<success_criteria>
- Phase success criterion 3 satisfied: the placeholder Farsi route serves a document whose root element is `<html lang="fa" dir="rtl">` while English pages still serve `lang="en"` left-to-right.
- ROUTE-02 re-proven under the final phase state: zero URLs lost, exactly `/fa` gained, and the English smoke baseline verifies clean.
- Phase success criterion 4 still holds: `ConditionalChrome.tsx` and `src/middleware.ts` still match the `frozen-files.sha` fingerprint.
- OQ-1 is resolved by observation rather than assumption: either the branded 404 survived, or the stop rule fired and the decision sits with Saeid unmodified.
</success_criteria>

<output>
Create `.planning/phases/01-route-groups/01-02-SUMMARY.md` when done. Record: the manifest delta (expected exactly `+/fa`), the `--verify` and `--fa-check` results, the observed `/fa` and `/` html tags verbatim, the OQ-1 404 outcome verbatim (status, html tag, branded marker present or not), the mobile-gate slug count, and the commit SHAs. If the stop rule fired, record the full observation and mark the plan blocked pending Saeid's decision rather than applying any contingency.
</output>