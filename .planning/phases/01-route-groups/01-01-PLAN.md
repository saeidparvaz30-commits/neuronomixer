---
phase: 01-route-groups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/checks/route-smoke.mjs
  - scripts/mobile-gate.mjs
  - scripts/fix-title-suffix.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/error.tsx
  - src/app/not-found.tsx
  - src/app/auth/
  - src/app/authors/
  - src/app/blog/
  - src/app/contact/
  - src/app/cv/
  - src/app/dashboard/
  - src/app/privacy/
  - src/app/review/
  - src/app/share/
  - src/app/studio/
  - src/app/visual-guides/
  - .planning/phases/01-route-groups/artifacts/routes-before.txt
  - .planning/phases/01-route-groups/artifacts/routes-after.txt
  - .planning/phases/01-route-groups/artifacts/route-smoke.baseline.json
  - .planning/phases/01-route-groups/artifacts/frozen-files.sha
  - .planning/phases/01-route-groups/01-VALIDATION.md
autonomous: true
requirements: [ROUTE-02]

must_haves:
  truths:
    - "The sorted set of values in .next/app-path-routes-manifest.json is byte-identical before and after the move (284 entries, zero added, zero removed)."
    - "npx tsc --noEmit exits 0 on a cold cache after the move."
    - "npx next build exits 0 after the move."
    - "Every route in the recorded smoke baseline returns the same status, redirect target, <html> tag, and chrome markers as it did before the move."
    - "An unmatched URL still returns HTTP 404 with the branded body marker and <html lang=\"en\">."
    - "A /cv/ URL still renders with no <nav> element (the ConditionalChrome bare-chrome special case is intact)."
    - "node scripts/mobile-gate.mjs --list-only prints 151 slugs, not 0."
    - "The move landed as exactly one commit containing only src/app renames plus the two script path fixes."
    - "src/middleware.ts, ConditionalChrome.tsx, next.config.ts, package.json, and both lockfiles have the same content hashes at the end of the plan as at the start."
  artifacts:
    - scripts/checks/route-smoke.mjs
    - .planning/phases/01-route-groups/artifacts/frozen-files.sha
    - .planning/phases/01-route-groups/artifacts/routes-before.txt
    - .planning/phases/01-route-groups/artifacts/routes-after.txt
    - .planning/phases/01-route-groups/artifacts/route-smoke.baseline.json
    - src/app/(en)/layout.tsx
    - src/app/(en)/page.tsx
    - src/app/(en)/error.tsx
    - src/app/(en)/not-found.tsx
    - src/app/(en)/studio/[[...tool]]/page.tsx
  key_links:
    - "src/app/(en)/layout.tsx imports @/styles/globals.css (alias form) — without it every English page renders unstyled."
    - "src/app/(en)/layout.tsx keeps NextAuthProvider wrapping ConditionalChrome — dropping it silently kills session context on every English page."
    - "src/app/(en)/studio/[[...tool]]/page.tsx reaches sanity.config through five parent levels — the @/ alias cannot express a repo-root file."
    - "Nine @/app/(en)/dashboard/... specifiers must resolve under both tsc and the Turbopack build (parentheses in a module specifier are new to this repo)."
    - "scripts/mobile-gate.mjs discovery path must resolve to the moved guide directory or the mobile gate silently passes on zero guides."
---

<objective>
Move every existing route in `src/app/` into the `src/app/(en)/` route group, fix the 13 path-encoding sites the move breaks, and prove that not one emitted URL or rendering behavior changed. This lands as exactly one commit.

Purpose: `src/app/layout.tsx` is currently the sole root layout. A second locale root layout cannot exist until the English tree lives inside its own route group. Route-group folders are stripped from the URL before matching, so this is URL-neutral by construction — but only if nothing that encodes the old path is left behind.

Output: `src/app/(en)/` holding 11 directories and 4 files (243 tracked files); `api/`, `sitemap.ts`, `robots.ts`, `icon.svg` still at the `src/app/` root; a new `scripts/checks/route-smoke.mjs`; committed before/after route-manifest evidence; one solo move commit.

Requirement: ROUTE-02 (existing English routes keep their exact URLs and rendering behavior).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-route-groups/01-RESEARCH.md
@.planning/phases/01-route-groups/01-PATTERNS.md
@.planning/phases/01-route-groups/01-VALIDATION.md
</context>

<hard_constraints>
Violating any of these fails the plan regardless of test results.

1. **NEVER run `npm run build`.** It chains `prisma migrate deploy` against the PRODUCTION database. The build gate is `npx next build`. `npx prisma generate` is safe (contacts no database); `prisma migrate` in any form is forbidden.
2. **No new dependencies.** No `npm install`, `pnpm add`, `npm i`, or `yarn add`. `package.json` and both lockfiles must end this plan byte-identical to HEAD.
3. **No push, no merge, no deploy.** Local commits only.
4. **Do not touch** `src/middleware.ts`, `src/components/appSkeleton/ConditionalChrome.tsx`, `next.config.ts`, `.env`, `.env.local`, `vercel.json`, or anything under `src/components/VisualGuides/`.
5. **No surrounding cleanup.** Do not rewrite the 9 alias imports into relative form, do not reformat, do not add comments to the 8 dashboard files, do not "improve" `ConditionalChrome`.
6. **Use `git mv`** so the index carries renames, not add/delete pairs.
7. **Quote every path containing `(en)`.** Parentheses are grouping operators in both bash and PowerShell.
8. **Never `git add -A` or `git add .`** in this plan. Stage by explicit pathspec only.
9. **No `Co-Authored-By:` line** in any commit message.
</hard_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Build the route smoke tool and capture the pre-move baselines</name>

  <files>
    scripts/checks/route-smoke.mjs (new),
    .planning/phases/01-route-groups/artifacts/routes-before.txt (new),
    .planning/phases/01-route-groups/artifacts/route-smoke.baseline.json (new)
  </files>

  <read_first>
    - `.planning/phases/01-route-groups/01-PATTERNS.md` section "scripts/checks/route-smoke.mjs (NEW, Wave 0)" — the composite style contract (docblock naming the run command, `GATE_BASE_URL` env override, `node:assert`, `--flag` parsing via `process.argv.slice(2)`, per-item `ok`/`FAIL` lines, `n/total passed` summary, `process.exit(1)` with a JSON failure dump).
    - `scripts/mobile-gate.mjs` — the CLI and base-URL conventions to copy verbatim in shape (lines 1-13 and 23-29).
    - `scripts/checks/shared-pdfs-lib.check.ts` — the assertion and `ALL PASS` exit style.
    - `.planning/phases/01-route-groups/01-RESEARCH.md` sections "Self-calibrating route smoke", "Validation Architecture", and Open Questions OQ-3 and OQ-4.
  </read_first>

  <action>
Write `scripts/checks/route-smoke.mjs`, then capture two baselines from the tree exactly as it stands at HEAD, before any file moves.

**Part A — the script.** Plain ESM `.mjs`, Node 20+ global `fetch`, `node:assert`, `node:fs`. Zero imports from `src/`. Zero new dependencies.

Top-of-file docblock must name the literal run commands and state that it requires a server on `GATE_BASE_URL`.

Constants:
- `BASE` = `process.env.GATE_BASE_URL` with fallback `http://localhost:3000` (this exact env var name — it is the repo convention and lets this gate and the mobile gate share one server).
- `BASELINE_PATH` = `.planning/phases/01-route-groups/artifacts/route-smoke.baseline.json`.
- `FIXED_ROUTES`, in this order: `/`, `/blog`, `/visual-guides`, `/authors`, `/contact`, `/privacy`, `/review`, `/auth/sign-in`, `/dashboard`, `/dashboard/admin/api-key`, `/dashboard/admin/suggest-category`, `/studio`, `/sitemap.xml`, `/robots.txt`, `/icon.svg`, `/cv/__smoke-check`, `/share/__smoke-check`, `/__unmatched-url-smoke-check`.

`probe(url)` — `await fetch(BASE + url, { redirect: "follow" })`, read the body as text, return an object with exactly these keys: `url`, `status`, `redirected`, `finalPath` (the `pathname` of `res.url`), `contentType` (the `content-type` response header, or `null`), `htmlTag` (the first match of the regular expression matching an opening `html` tag, or `null` when the body is not HTML), `hasNav` (body includes the literal opening nav tag), `hasFooter` (body includes the literal opening footer tag), `hasBrandedNotFound` (body includes `Page not found`). Probe sequentially, not with `Promise.all`, so a cold server is not hammered.

`discoverRoutes()` — used only by `--record`:
- Fetch `/blog`, collect every href matching a blog path with the regular expression for `href="(/blog/[^"?#]+)"`, dedupe preserving document order. Take the first entry with exactly two segments after `/blog` (a post) and the first with exactly one segment (a category). If either is missing, exit 1 with a message naming what was not found.
- Fetch `/visual-guides`, collect every href matching `href="(/visual-guides/[^"?#]+)"`, dedupe preserving document order. The index renders guides grouped by category in document order, so sample across it: take indices `0`, and then every `Math.floor(n / 7)`-th entry, capping at 8 URLs. Assert at least 5 distinct guide URLs were collected; if fewer, exit 1 with a message (a shrunken index means the discovery regex broke, and a smoke list of one guide proves nothing).
- Return the fixed list concatenated with the discovered blog and guide URLs.

`assertAbsolutes(results)` — runs in BOTH modes, using `node:assert` with a message string on every assertion:
- The `/__unmatched-url-smoke-check` result: `status === 404`; `hasBrandedNotFound === true`; its `htmlTag` includes `lang="en"` and does not include a `dir` attribute.
- The `/cv/__smoke-check` result: `hasNav === false` (this is the proof that `ConditionalChrome`'s bare-chrome special case still fires).
- The `/` result: `htmlTag` is exactly the opening html tag with `lang="en"` and no other attributes.

`--record` mode: build the route list via `discoverRoutes()`, probe every entry, run `assertAbsolutes`, write `{ recordedAt, base, routes }` as pretty-printed JSON to `BASELINE_PATH` (creating the directory if needed), print `route-smoke: RECORDED <n> routes`.

`--verify` mode: read and parse `BASELINE_PATH`; probe exactly `baseline.routes.map(r => r.url)` and nothing else (so the two runs always compare the identical URL set); for each, deep-compare every recorded key except `url`; write a per-route `ok  <url>` or `FAIL <url>` line with `process.stdout.write`; run `assertAbsolutes`; print `<passed>/<total> passed`; if there are failures, `console.log(JSON.stringify(failures, null, 2))` then `process.exit(1)`; otherwise print `route-smoke.mjs: ALL PASS`.

No mode flag, or an unknown flag: print the usage lines and `process.exit(1)`.

**Part B — freeze the must-not-change files.** Before anything else, record a content fingerprint of the six files this phase is forbidden to touch, so later tasks can prove non-modification independently of the working tree's starting state:

`git hash-object src/middleware.ts src/components/appSkeleton/ConditionalChrome.tsx next.config.ts package.json package-lock.json pnpm-lock.yaml` redirected to `.planning/phases/01-route-groups/artifacts/frozen-files.sha` (six lines, one blob hash per file, in that order).

Do NOT use `git diff HEAD` for this purpose. The working tree already carries pre-existing uncommitted drift that has nothing to do with this phase: `package-lock.json` holds an uncommitted transitive Sanity client bump, and `pnpm-lock.yaml` and `pnpm-workspace.yaml` are untracked leftovers from an earlier package-manager experiment. A HEAD-relative check would report a false failure. The fingerprint freezes whatever state exists at the start of the phase and proves only that this phase changed nothing. Do not attempt to clean up that drift; it is out of scope and touching lockfiles is forbidden here.

**Part C — cold pre-move gate and baselines.** Run from the repo root, in this order:

1. Delete the stale caches: remove `.next` and `tsconfig.tsbuildinfo`. Both are gitignored. `tsconfig.json` sets `"incremental": true` and the existing `tsconfig.tsbuildinfo` is from July; skipping this step lets a later `tsc` report a false clean off a stale program graph.
2. `npx tsc --noEmit` — must exit 0. This is the green-at-HEAD proof, so any post-move failure can only have been caused by the move.
3. `npx next build` — must exit 0. Never `npm run build`. If it fails with a message about `@prisma/client` not being initialized, run `npx prisma generate` (safe, contacts no database) and retry the build once.
4. Extract the baseline URL set: run node with `-e` to read `./.next/app-path-routes-manifest.json`, take `Object.values(...)`, `.sort()`, join on newlines, and redirect stdout to `.planning/phases/01-route-groups/artifacts/routes-before.txt`. Expect exactly 284 lines. The keys of this manifest are app-tree file paths and will change; only the values are the contract.
5. Start the production server in the background: `npx next start`. Poll `http://localhost:3000/robots.txt` until it responds or 90 seconds elapse. If port 3000 is occupied, start with `-p 3100` and set `GATE_BASE_URL=http://localhost:3100` for the smoke run.
6. `node scripts/checks/route-smoke.mjs --record` — must exit 0 and write the baseline JSON. Record against the production server, not `next dev`, so the post-move comparison is production-accurate and both sides of the diff use the same server mode.
7. Stop the background server.
8. Commit the tooling and the baselines together, before the move, so the move commit stays solo. Stage by explicit pathspec: `scripts/checks/route-smoke.mjs` and `.planning/phases/01-route-groups/artifacts/` (which now holds `frozen-files.sha`, `routes-before.txt`, and `route-smoke.baseline.json`). Never `git add -A`; the pre-existing lockfile drift must stay out of every commit in this phase. Commit message: `chore(01-01): add route smoke check and capture pre-move baselines`. No `Co-Authored-By` line.

If step 6's absolute assertions fail at HEAD, STOP and report. A pre-existing broken branded 404 or a pre-existing `<nav>` on a `/cv/` URL changes what this phase is allowed to conclude, and it is not this plan's job to fix it.
  </action>

  <verify>
    <automated>node scripts/checks/route-smoke.mjs; test $? -eq 1 &amp;&amp; wc -l .planning/phases/01-route-groups/artifacts/routes-before.txt &amp;&amp; node -e "const b=require('./.planning/phases/01-route-groups/artifacts/route-smoke.baseline.json');const u=b.routes.map(r=>r.url);const g=u.filter(x=>x.startsWith('/visual-guides/'));if(u.length&lt;24)throw new Error('too few routes: '+u.length);if(g.length&lt;5)throw new Error('too few guides: '+g.length);if(!u.includes('/__unmatched-url-smoke-check'))throw new Error('404 probe missing');console.log('baseline ok',u.length,'routes,',g.length,'guides')"</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` at HEAD exits 0.
    - `npx next build` at HEAD exits 0. `npm run build` was never invoked.
    - `wc -l .planning/phases/01-route-groups/artifacts/routes-before.txt` prints `284`.
    - `.planning/phases/01-route-groups/artifacts/route-smoke.baseline.json` parses as JSON and its `routes` array has at least 24 entries, of which at least 5 have a URL beginning `/visual-guides/`, at least 1 matches a two-segment `/blog/` path, and one is exactly `/__unmatched-url-smoke-check`.
    - The baseline entry for `/__unmatched-url-smoke-check` has `status` 404, `hasBrandedNotFound` true, and an `htmlTag` containing `lang="en"`.
    - The baseline entry for `/cv/__smoke-check` has `hasNav` false.
    - `node scripts/checks/route-smoke.mjs` with no flag exits 1 and prints usage.
    - `grep -c 'GATE_BASE_URL' scripts/checks/route-smoke.mjs` is at least 1.
    - `.planning/phases/01-route-groups/artifacts/frozen-files.sha` exists and `wc -l` prints `6`.
    - `git log -1 --pretty=%s` is `chore(01-01): add route smoke check and capture pre-move baselines`, and `git show --name-only --pretty=format: HEAD` lists only paths under `scripts/checks/` and `.planning/`.
    - `git log -1 --pretty=%B | grep -ci 'co-authored-by'` is 0.
  </acceptance_criteria>

  <done>
    `scripts/checks/route-smoke.mjs` exists and is committed; `routes-before.txt` holds 284 sorted URLs; `route-smoke.baseline.json` holds a recorded probe of at least 24 concrete URLs including the 404 probe and the `/cv/` probe; the tree is proven green at HEAD; nothing under `src/app/` has moved yet.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move the English tree into (en) and fix all 13 path-encoding sites</name>

  <files>
    src/app/(en)/ (11 directories + 4 files, 243 tracked files moved),
    src/app/(en)/layout.tsx,
    src/app/(en)/studio/[[...tool]]/page.tsx,
    src/app/(en)/dashboard/admin/api-key/page.tsx,
    src/app/(en)/dashboard/admin/cv/designer/page.tsx,
    src/app/(en)/dashboard/admin/cv/page.tsx,
    src/app/(en)/dashboard/admin/my-posts/page.tsx,
    src/app/(en)/dashboard/admin/my-posts/[id]/edit/page.tsx,
    src/app/(en)/dashboard/admin/submit/page.tsx,
    src/app/(en)/dashboard/admin/suggest-category/page.tsx,
    src/app/(en)/dashboard/author/posts/[id]/edit/EditPostFormLoader.tsx,
    scripts/mobile-gate.mjs,
    scripts/fix-title-suffix.ts
  </files>

  <read_first>
    - `.planning/phases/01-route-groups/01-RESEARCH.md` — the "Move/Stay Inventory" table (the authoritative disposition of all 12 top-level directories and 7 top-level files), Pitfall 1, Pitfall 2, Pitfall 3, Pitfall 4, and Pitfall 8.
    - `.planning/phases/01-route-groups/01-PATTERNS.md` — the `src/app/(en)/layout.tsx` section (the byte-identical block from line 2 to line 79, the provider/chrome nesting that must survive) and the two dashboard edit shapes.
    - `src/app/layout.tsx` — read in full before moving it; you are asserting afterwards that exactly one of its 79 lines changed.
    - `scripts/mobile-gate.mjs` lines 15-21 — the `allSlugs()` discovery function.
    - `scripts/fix-title-suffix.ts` lines 24-30 — the `collectPageFiles` call and the home-page skip guard.
  </read_first>

  <action>
Do NOT commit in this task. Task 3 creates the single commit for everything done here.

**Part A — the move.** From the repo root. Quote every path containing `(en)`.

Create the group directory: `mkdir "src/app/(en)"`.

`git mv` these 11 directories from `src/app/<name>` to `src/app/(en)/<name>`, in this exact set: `auth`, `authors`, `blog`, `contact`, `cv`, `dashboard`, `privacy`, `review`, `share`, `studio`, `visual-guides`.

`git mv` these 4 files from `src/app/<name>` to `src/app/(en)/<name>`: `layout.tsx`, `page.tsx`, `error.tsx`, `not-found.tsx`.

These 4 entries STAY at `src/app/` and must not be moved or duplicated: `api/` (Route Handlers are not wrapped by any layout, and the API is language-neutral), `sitemap.ts`, `robots.ts`, `icon.svg` (all three are special Route Handlers serving the whole origin; a second copy inside a group would produce two routes resolving to the same URL, which is a build-time Conflicting paths error).

`src/app/layout.tsx` must not survive at the root. Any layout above the groups would make neither group's layout a root layout and would nest the future `(fa)` html document inside the `(en)` one.

After the move, `ls src/app/` must print exactly five entries: `(en)`, `api`, `icon.svg`, `robots.ts`, `sitemap.ts`.

**Part B — the two parent-escaping relative imports.** The approved design spec claims no relative import can break. That claim is wrong; these two do, because inserting `(en)/` adds one directory level.

- `src/app/(en)/layout.tsx` line 1: the bare side-effect stylesheet import currently written as a single-parent relative path to `styles/globals.css` becomes the alias form `@/styles/globals.css`. Use the alias, not a corrected relative path, so a future group move cannot break it again. Tailwind v4 content detection is rooted at the CSS file, not the importer, so nothing else changes. Change nothing else in this file: lines 2 through 79 must survive byte-identical, including the `NextAuthProvider` wrapper around `ConditionalChrome`, `GeneralSignupPrompt`, `FlushPendingCompletions`, `GoogleAnalyticsTracker`, and `SpeedInsights`. Dropping any of those silently breaks auth session context or analytics on every English page.
- `src/app/(en)/studio/[[...tool]]/page.tsx` line 11: `import config from "../../../../sanity.config";` becomes `import config from "../../../../../sanity.config";` — five parent levels, not four. The `@/` alias cannot express this: it maps to `./src/*` and `sanity.config.ts` sits at the repo root, outside `src/`. This is the only file in the moved tree where relative form is correct.

**Part C — the nine `@/app/...` alias specifiers.** `@/` maps to `./src/*`, so these are paths, not logical names, and all nine now point at a directory that no longer exists. The edit is mechanical prefix insertion and nothing else: the segment `app/` in the specifier becomes `app/(en)/`. Do not re-point any of them, do not convert them to relative form, do not reformat, and add no comments — the acceptance criterion counts occurrences and any extra mention breaks it. Use this list verbatim; do not re-grep, because a fresh grep is how the ninth site gets missed.

  1. `src/app/(en)/dashboard/admin/api-key/page.tsx` line 3 — `export { default } from "@/app/dashboard/author/api-key/page";` becomes `export { default } from "@/app/(en)/dashboard/author/api-key/page";`
  2. `src/app/(en)/dashboard/admin/cv/designer/page.tsx` line 4 — `import CVDesignerClient from "@/app/dashboard/author/cv/designer/CVDesignerClient";` becomes the same import with `@/app/(en)/dashboard/author/cv/designer/CVDesignerClient`
  3. `src/app/(en)/dashboard/admin/cv/page.tsx` line 4 — `import CVBuilderClient from "@/app/dashboard/author/cv/CVBuilderClient";` becomes the same import with `@/app/(en)/dashboard/author/cv/CVBuilderClient`
  4. `src/app/(en)/dashboard/admin/my-posts/page.tsx` line 6 — `import { PostActionButtons } from "@/app/dashboard/author/posts/PostActionButtons";` becomes the same import with `@/app/(en)/dashboard/author/posts/PostActionButtons`
  5. `src/app/(en)/dashboard/admin/my-posts/[id]/edit/page.tsx` line 5 — `import EditPostFormLoader from "@/app/dashboard/author/posts/[id]/edit/EditPostFormLoader";` becomes the same import with `@/app/(en)/dashboard/author/posts/[id]/edit/EditPostFormLoader`
  6. `src/app/(en)/dashboard/admin/submit/page.tsx` line 2 — `import SubmitPostForm from "@/app/dashboard/author/submit/SubmitPostForm";` becomes the same import with `@/app/(en)/dashboard/author/submit/SubmitPostForm`
  7. `src/app/(en)/dashboard/admin/suggest-category/page.tsx` line 1 — `export { default } from "@/app/dashboard/author/suggest-category/page";` becomes the same re-export with `@/app/(en)/dashboard/author/suggest-category/page`
  8. `src/app/(en)/dashboard/author/posts/[id]/edit/EditPostFormLoader.tsx` line 5 — `import type SubmitPostFormType from "@/app/dashboard/author/submit/SubmitPostForm";` becomes the same type import with `@/app/(en)/dashboard/author/submit/SubmitPostForm`
  9. `src/app/(en)/dashboard/author/posts/[id]/edit/EditPostFormLoader.tsx` line 11 — the dynamic `() => import("@/app/dashboard/author/submit/SubmitPostForm"),` inside the `next/dynamic` call becomes `() => import("@/app/(en)/dashboard/author/submit/SubmitPostForm"),`. This is the dangerous one: it is a lazy chunk, so a stale specifier can fail at runtime on the post-edit page rather than at the type check. The build resolves it too, but browser-verify this route in Task 3's human check.

Sites 1 and 7 are pure cross-route re-exports from an admin path to an author path. Mis-pointing either would hand an admin surface the wrong component, so copy the target paths exactly as written above.

Parentheses are legal path characters but this repo has never carried one inside a module specifier. If `tsc` or the build rejects them, STOP and report rather than improvising: the documented fallback is relative imports within the moved tree, and that is a decision, not a fix.

**Part D — the two scripts outside `src/app`.** Both encode the old path and both are mechanical consequences of the move, so they ride in the same commit.

- `scripts/mobile-gate.mjs` line 16, inside `allSlugs()`: the `path.join` call currently passes the segments `process.cwd()`, `"src"`, `"app"`, `"visual-guides"`. Insert `"(en)"` as its own argument between `"app"` and `"visual-guides"`. Keep the existing style of separate `path.join` arguments; never build a slash-joined string. Leaving this unfixed is the worst outcome in this plan: the gate would discover zero guides and can report a green `0/0 passed`.
- `scripts/fix-title-suffix.ts` line 29: the home-page skip guard compares the normalized file path against `"src/app/page.tsx"`. Change that string literal to `"src/app/(en)/page.tsx"`. Keep the trailing comment exactly as it is. Line 24's `collectPageFiles("src/app")` needs no change — it recurses and simply descends one more level.

**Part E — self-check before handing off.** Run `git status` and confirm the moved files appear under `renamed:`, not as separate deletions and additions. Then re-run the Task 1 fingerprint command over the same six paths in the same order and pipe it into `diff - .planning/phases/01-route-groups/artifacts/frozen-files.sha`; it must produce no output. That proves `src/middleware.ts`, `ConditionalChrome.tsx`, `next.config.ts`, `package.json`, and both lockfiles are byte-identical to the phase's starting state. Ignore the pre-existing lockfile drift described in Task 1 Part B; it is frozen into the fingerprint and is not this phase's concern.
  </action>

  <verify>
    <automated>test "$(ls src/app/ | sort | tr '\n' ' ')" = "(en) api icon.svg robots.ts sitemap.ts " &amp;&amp; test "$(grep -c '@/styles/globals.css' 'src/app/(en)/layout.tsx')" = "1" &amp;&amp; test "$(grep -c '\.\./\.\./\.\./\.\./\.\./sanity.config' 'src/app/(en)/studio/[[...tool]]/page.tsx')" = "1" &amp;&amp; test "$(grep -rn '@/app/(en)/dashboard/' src/app/ | wc -l | tr -d ' ')" = "9" &amp;&amp; test "$(grep -c '"(en)"' scripts/mobile-gate.mjs)" = "1" &amp;&amp; test "$(grep -c 'src/app/(en)/page.tsx' scripts/fix-title-suffix.ts)" = "1" &amp;&amp; git hash-object src/middleware.ts src/components/appSkeleton/ConditionalChrome.tsx next.config.ts package.json package-lock.json pnpm-lock.yaml | diff - .planning/phases/01-route-groups/artifacts/frozen-files.sha &amp;&amp; echo "MOVE STRUCTURE OK"</automated>
  </verify>

  <acceptance_criteria>
    - `ls src/app/` prints exactly and only: `(en)`, `api`, `icon.svg`, `robots.ts`, `sitemap.ts`.
    - `git ls-files "src/app/(en)" | wc -l` prints `243`.
    - `git ls-files src/app | wc -l` prints `319` (nothing was lost or added).
    - `test -f src/app/layout.tsx` fails (the old root layout no longer exists at the root).
    - `git status --porcelain "src/app" | grep -c '^R'` is at least `240` (renames, not add/delete pairs).
    - `grep -c '@/styles/globals.css' "src/app/(en)/layout.tsx"` is `1`.
    - `grep -c 'NextAuthProvider' "src/app/(en)/layout.tsx"` is `2` (the import and the JSX wrapper both survived).
    - `wc -l "src/app/(en)/layout.tsx"` is unchanged from the pre-move file.
    - `grep -c '\.\./\.\./\.\./\.\./\.\./sanity.config' "src/app/(en)/studio/[[...tool]]/page.tsx"` is `1`.
    - `grep -rn '@/app/(en)/dashboard/' src/app/ | wc -l` is exactly `9`.
    - `grep -c '"(en)"' scripts/mobile-gate.mjs` is `1`.
    - `grep -c 'src/app/(en)/page.tsx' scripts/fix-title-suffix.ts` is `1`.
    - `git hash-object src/middleware.ts src/components/appSkeleton/ConditionalChrome.tsx next.config.ts package.json package-lock.json pnpm-lock.yaml | diff - .planning/phases/01-route-groups/artifacts/frozen-files.sha` produces no output and exits 0.
    - `git log -1 --pretty=%s` still prints the Task 1 chore subject (no commit was created in this task).
  </acceptance_criteria>

  <done>
    243 tracked files live under `src/app/(en)/`; `api/`, `sitemap.ts`, `robots.ts`, `icon.svg` remain at the `src/app/` root; all 13 path-encoding sites are fixed; the working tree is staged as renames and is uncommitted, awaiting the Task 3 gate.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run the cold gate, prove URL equality, and land the solo move commit</name>

  <files>
    .planning/phases/01-route-groups/artifacts/routes-after.txt (new),
    .planning/phases/01-route-groups/01-VALIDATION.md
  </files>

  <read_first>
    - `.planning/phases/01-route-groups/01-RESEARCH.md` sections "The URL-equality proof (the ROUTE-02 gate)", "Sampling Rate", "How byte-for-byte URL preservation is verified — concretely", and Pitfall 5.
    - `.planning/phases/01-route-groups/01-VALIDATION.md` — the Per-Task Verification Map table you are completing, and the Manual-Only Verifications table.
    - `.planning/phases/01-route-groups/artifacts/routes-before.txt` — the 284-line ground truth this task diffs against.
  </read_first>

  <action>
Verify first, commit second. The success criterion is a solo commit gated on a green build, and the global rules discourage `--amend`, so a failure discovered after committing cannot be repaired cleanly.

**Part A — the cold gate.** In this order, from the repo root:

1. Remove `.next` and `tsconfig.tsbuildinfo` again. After renaming 243 files an incremental `tsc` can reason off a stale program graph and report a false clean. If `tsc` reports zero errors on a tree you have not yet fixed, the cache lied.
2. `npx tsc --noEmit` — must exit 0. This is what catches the 11 moved-path specifiers.
3. `npx next build` — must exit 0. Never `npm run build`. The lint gate runs inside it (`next.config.ts` sets `eslint.ignoreDuringBuilds: false` and `typescript.ignoreBuildErrors: false`), so a lint failure fails this step too.

**Part B — the URL-equality proof.** Extract `Object.values(...)` from `./.next/app-path-routes-manifest.json`, sort, join on newlines, write to `.planning/phases/01-route-groups/artifacts/routes-after.txt`. Diff it against `routes-before.txt`. The diff must be empty. An empty diff is byte-exact proof that all 284 emitted URLs are unchanged, including all 151 guide routes and all 73 API routes. The manifest *keys* will have changed (an app-tree path gains the group segment) and that is precisely the evidence the group is invisible; only the values are the contract. If the diff is non-empty, STOP and reconcile against the Move/Stay table before doing anything else.

**Part C — the rendering-behavior proof.** Start `npx next start` in the background, poll `/robots.txt` until it responds or 90 seconds elapse, then run `node scripts/checks/route-smoke.mjs --verify`. It re-fetches exactly the URL set recorded in Task 1 and diffs every recorded field, so this covers the criterion-1 route list (a spread of guides, `/blog`, a blog post, `/visual-guides`, a `/cv/` URL, a `/share/` URL, the dashboard) plus the branded-404 and bare-chrome absolute assertions. Must print `ALL PASS` and exit 0. Stop the server afterwards.

**Part D — the guide-discovery proof.** `node scripts/mobile-gate.mjs --list-only` must print exactly 151 lines. A count of 0 means the path fix did not land; a green mobile gate that tested nothing is worse than a red one. This needs no server.

**Part E — the untouched-file proof.** Re-run the six-path `git hash-object` fingerprint from Task 1 and pipe it into `diff - .planning/phases/01-route-groups/artifacts/frozen-files.sha`. It must produce no output. That discharges phase success criterion 4 (`src/middleware.ts` and `ConditionalChrome.tsx` untouched) and the no-install and no-config-change constraints in one check, without being confused by the pre-existing lockfile drift.

**Part F — the solo commit.** Stage by explicit pathspec only. Never `git add -A` and never `git add .`; `routes-after.txt` is sitting untracked in the working tree and must not enter this commit. The renames are already in the index from `git mv`; add the content edits with `git add -u -- src/app scripts/mobile-gate.mjs scripts/fix-title-suffix.ts`. Then commit with subject `refactor(01-01): move all existing routes into the (en) route group` and a body noting that the two script path fixes ride along as mechanical consequences of the move, and that `api/`, `sitemap.ts`, `robots.ts`, and `icon.svg` stay at the app root. No `Co-Authored-By` line.

Verify the commit's shape immediately: `git show --name-only --pretty=format: HEAD` must list only paths under `src/app/` and `scripts/`, and `git show -M --summary HEAD` must show 243 rename lines.

**Part G — the evidence commit.** Separately, stage and commit `.planning/phases/01-route-groups/artifacts/routes-after.txt` and the updated `01-VALIDATION.md` with subject `docs(01-01): record post-move route manifest evidence`.

**Part H — update the validation map.** `.planning/phases/01-route-groups/01-VALIDATION.md` already carries a Per-Task Verification Map row for every task in both plans. Flip the Status cell for rows `01-01-T1`, `01-01-T2`, and `01-01-T3` from pending to green, and set `wave_0_complete: true` in its frontmatter now that `scripts/checks/route-smoke.mjs` exists. Do not restructure the table or change any other row.
  </action>

  <verify>
    <automated>rm -rf .next tsconfig.tsbuildinfo &amp;&amp; npx tsc --noEmit &amp;&amp; npx next build &amp;&amp; node -e "console.log(Object.values(require('./.next/app-path-routes-manifest.json')).sort().join('\n'))" > .planning/phases/01-route-groups/artifacts/routes-after.txt &amp;&amp; diff .planning/phases/01-route-groups/artifacts/routes-before.txt .planning/phases/01-route-groups/artifacts/routes-after.txt &amp;&amp; test "$(node scripts/mobile-gate.mjs --list-only | wc -l | tr -d ' ')" = "151" &amp;&amp; git hash-object src/middleware.ts src/components/appSkeleton/ConditionalChrome.tsx next.config.ts package.json package-lock.json pnpm-lock.yaml | diff - .planning/phases/01-route-groups/artifacts/frozen-files.sha &amp;&amp; echo "ROUTE-02 PROVEN: 0 URL changes, 151 guides discoverable, frozen files intact"</automated>
    <automated>node scripts/checks/route-smoke.mjs --verify</automated>
    <human-check>
      In Saeid's authenticated Chrome against a local `npx next start` on port 3000, confirm the lazy chunk that the type checker cannot fully vouch for still loads: sign in, open `/dashboard/author/posts/&lt;an existing post id&gt;/edit`, and confirm the submit form renders with no chunk-load error in the console. Then spot-check `/dashboard/admin/api-key` and `/dashboard/admin/suggest-category` (the two pure cross-route re-exports) render the author-side pages they re-export.
    </human-check>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 on a cold cache (`.next` and `tsconfig.tsbuildinfo` were deleted immediately beforehand).
    - `npx next build` exits 0. `npm run build` was never invoked in this plan.
    - `diff .planning/phases/01-route-groups/artifacts/routes-before.txt .planning/phases/01-route-groups/artifacts/routes-after.txt` produces no output and exits 0.
    - `wc -l .planning/phases/01-route-groups/artifacts/routes-after.txt` prints `284`.
    - `node scripts/checks/route-smoke.mjs --verify` exits 0 and its final line is `route-smoke.mjs: ALL PASS`.
    - `node scripts/mobile-gate.mjs --list-only | wc -l` prints `151`.
    - `git hash-object src/middleware.ts src/components/appSkeleton/ConditionalChrome.tsx next.config.ts package.json package-lock.json pnpm-lock.yaml | diff - .planning/phases/01-route-groups/artifacts/frozen-files.sha` produces no output and exits 0.
    - `git show --name-only --pretty=format: HEAD~1 | grep -cv -e '^src/app/' -e '^scripts/' -e '^$'` is `0` after the evidence commit lands (the move commit contains only app and script paths).
    - `git show -M --summary HEAD~1 | grep -c '^ rename '` is `243`.
    - `git show -M --numstat HEAD~1 -- "src/app/(en)/layout.tsx"` reports 1 line added and 1 line deleted.
    - `git log --oneline -2 --pretty=%s` prints the evidence subject then `refactor(01-01): move all existing routes into the (en) route group`.
    - `git log -2 --pretty=%B | grep -ci 'co-authored-by'` is `0`.
    - `.planning/phases/01-route-groups/01-VALIDATION.md` frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`, and the rows `01-01-T1`, `01-01-T2`, `01-01-T3` in its Per-Task Verification Map show a green status.
  </acceptance_criteria>

  <done>
    A cold `tsc` and `npx next build` both exit 0; the sorted manifest values are byte-identical to the pre-move baseline; the route smoke verifies clean including the branded 404 and the `/cv/` bare-chrome case; the mobile gate discovers 151 guides; `src/middleware.ts` and `ConditionalChrome.tsx` are untouched; the move is one commit containing only `src/app/` renames and the two script fixes.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
Created by this plan:
- `scripts/checks/route-smoke.mjs` — new; modes `--record` and `--verify`; exports nothing (CLI script); reads `GATE_BASE_URL`; writes and reads `.planning/phases/01-route-groups/artifacts/route-smoke.baseline.json`
- `.planning/phases/01-route-groups/artifacts/routes-before.txt` — 284 sorted URL values, pre-move
- `.planning/phases/01-route-groups/artifacts/routes-after.txt` — 284 sorted URL values, post-move
- `.planning/phases/01-route-groups/artifacts/route-smoke.baseline.json` — recorded probe of ~24-26 concrete URLs
- `src/app/(en)/` — new route-group directory (URL-invisible)

Moved paths (243 tracked files, all renames):
- `src/app/layout.tsx` → `src/app/(en)/layout.tsx`
- `src/app/page.tsx` → `src/app/(en)/page.tsx`
- `src/app/error.tsx` → `src/app/(en)/error.tsx`
- `src/app/not-found.tsx` → `src/app/(en)/not-found.tsx`
- `src/app/{auth,authors,blog,contact,cv,dashboard,privacy,review,share,studio,visual-guides}/` → `src/app/(en)/{…}/`

Unmoved paths (76 tracked files, deliberately at the `src/app/` root): `api/`, `sitemap.ts`, `robots.ts`, `icon.svg`

Changed import specifiers (13 sites, 10 files):
- `src/app/(en)/layout.tsx` — the stylesheet side-effect import becomes the `@/styles/globals.css` alias form
- `src/app/(en)/studio/[[...tool]]/page.tsx` — the `sanity.config` relative import gains a fifth parent level
- 9 specifiers across 8 files under `src/app/(en)/dashboard/` — the `app/` segment becomes `app/(en)/`
- `scripts/mobile-gate.mjs` — `allSlugs()` discovery path gains an `"(en)"` segment
- `scripts/fix-title-suffix.ts` — the home-page skip guard literal gains an `(en)` segment

URLs added or removed: none. That is the point of the plan.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous internet → `/dashboard/*` | `src/middleware.ts` enforces role and suspended-user gating on a URL matcher; the move must not change the URL surface it matches |
| Anonymous internet → `/studio` | Sanity Studio, `Disallow`ed in production robots and behind Sanity auth |
| Crawler → `/robots.txt` | `robots.ts` gates `Disallow: /` on `VERCEL_ENV === "production"`; if the route stops emitting, preview environments become crawlable |
| Repo → dependency tree | No install runs in this phase; any package-manager write is out of scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing | `src/app/(en)/layout.tsx` | high | mitigate | `NextAuthProvider` must still wrap `ConditionalChrome` after the move; dropping it silently voids session context on every English page. Gated by: numstat on the moved layout showing exactly 1 added / 1 deleted line, `grep -c 'NextAuthProvider'` equal to 2, and the `/dashboard` smoke probe recording the same redirect to `/auth/sign-in` as the baseline. |
| T-01-02 | Elevation of Privilege | `src/app/(en)/dashboard/admin/api-key/page.tsx`, `.../admin/suggest-category/page.tsx` | high | mitigate | These two are pure cross-route re-exports from an admin path to an author path. A mis-pointed rewrite hands an admin surface the wrong component. Mitigation: the 9 target paths are listed verbatim in Task 2 with prefix insertion only and no re-pointing; occurrence count asserted at exactly 9; both routes are in the smoke list and in the Task 3 human check. |
| T-01-03 | Elevation of Privilege | `src/middleware.ts` | medium | mitigate | The auth matcher is a URL pattern. The manifest diff proves zero URL changes, and the `frozen-files.sha` content fingerprint proves the file itself is byte-identical to its state at the start of the phase. |
| T-01-04 | Information Disclosure | `src/app/robots.ts` | medium | mitigate | `robots.ts` stays at the app root rather than moving into `(en)`. `/robots.txt` appears in both the manifest diff and the smoke baseline, so a 404 regression fails the gate. |
| T-01-05 | Information Disclosure | `src/app/(en)/studio/[[...tool]]/page.tsx` | low | mitigate | The five-level relative import fix could break the Studio route. `/studio` is in the smoke list; its status must match the baseline exactly. |
| T-01-06 | Denial of Service | whole route tree | high | mitigate | Silent route loss is the phase's real risk. Mitigation is the sorted-value diff of `app-path-routes-manifest.json` (284 entries) plus a cold `npx next build` at 0. |
| T-01-07 | Denial of Service | `scripts/mobile-gate.mjs` | medium | mitigate | If the discovery path is not fixed, the mobile gate can report a green `0/0 passed` forever. Gated by asserting `--list-only` prints exactly 151 lines. |
| T-01-SC | Tampering | package manager | high | mitigate | This phase installs nothing. No `npm install` / `pnpm add` / `yarn add` task exists, and RESEARCH.md records "Package Legitimacy Audit: not applicable — this phase installs zero external packages." Gated by the `frozen-files.sha` content fingerprint over `package.json`, `package-lock.json`, and `pnpm-lock.yaml`. Note the working tree already carries pre-existing uncommitted lockfile drift from before this phase; the fingerprint freezes that starting state so the gate proves only that this phase changed nothing. Any proposed install must be escalated to Saeid, not executed. |
| T-01-DB | Tampering | production Postgres | high | mitigate | `npm run build` chains `prisma migrate deploy` against production. Forbidden by hard constraint 1; the build gate is `npx next build`. `npx prisma generate` is the only permitted Prisma command and contacts no database. |
</threat_model>

<verification>
1. Cold `npx tsc --noEmit` exits 0.
2. Cold `npx next build` exits 0 (lint gate included).
3. `diff routes-before.txt routes-after.txt` is empty — 284 URLs proven unchanged.
4. `node scripts/checks/route-smoke.mjs --verify` prints `ALL PASS`.
5. `node scripts/mobile-gate.mjs --list-only | wc -l` prints 151.
6. The six-path `git hash-object` fingerprint still matches `frozen-files.sha` (middleware, ConditionalChrome, next.config.ts, package.json, both lockfiles).
7. The move is one commit; `git show -M --summary` shows 243 renames and no path outside `src/app/` or `scripts/`.
8. Human check: the lazy `next/dynamic` chunk on the post-edit page loads in Saeid's authenticated Chrome.
</verification>

<success_criteria>
- ROUTE-02 discharged mechanically: the sorted set of emitted URLs is byte-identical before and after the move.
- Phase success criterion 2 satisfied: the `(en)` move is its own solo commit, gated on `npx tsc --noEmit` at 0 and `npx next build` at 0 before the commit was created.
- Phase success criterion 4 satisfied: `ConditionalChrome.tsx` and `src/middleware.ts` are byte-identical to their pre-phase content, proven by the `frozen-files.sha` fingerprint.
- The tree is ready for a second root layout: no `layout.tsx` remains at `src/app/`.
</success_criteria>

<output>
Create `.planning/phases/01-route-groups/01-01-SUMMARY.md` when done. Record: the 284-entry manifest diff result, the smoke pass count, the mobile-gate slug count, the two commit SHAs (move and evidence), and any deviation from the Move/Stay inventory.
</output>
