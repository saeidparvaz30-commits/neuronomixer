---
phase: 03-translation-pipeline
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - .planning/phases/03-translation-pipeline/artifacts/deploy-gate.md
autonomous: false
requirements: [PIPE-01]

must_haves:
  truths:
    - "Saeid explicitly authorised the push of main and the production deploy before either happened (D-11 step 2)"
    - "The Phase 2 English-language filter is live on production and the production build was regenerated after the push"
    - "The 18-field postType, including sourceUpdatedAt, is live in the production Studio"
    - "No Farsi document exists in blog_posts at the moment the gate closes, so the Phase 2 ordering invariant held"
  artifacts:
    - ".planning/phases/03-translation-pipeline/artifacts/deploy-gate.md"
  key_links:
    - "This gate is the precondition for plan 03-10; no run may touch the production dataset until it closes (D-11 step 3)"
    - "The lockfile repaired in plan 03-01 is what Vercel installs on this deploy, so this deploy is also the evidence that the three too-new SUS package bumps are benign (T-03-11)"
---

<objective>
Close D-11 step 2: get roughly sixty local commits of Phase 1 and Phase 2 work onto `main` and deployed to production, with Saeid's explicit in-session authorisation, and prove afterwards that the English-language filter and the updated post schema are live on production.

Purpose: the Phase 2 ordering invariant says the English filter must be live BEFORE the first Farsi document exists. Right now the filter is committed locally and not deployed. Until this gate closes, a production Farsi draft could surface in an English listing. It is also the deploy that proves the repaired lockfile installs cleanly on Vercel.
Output: production running the Phase 2 filter and the 18-field schema, with the evidence recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-translation-pipeline/03-CONTEXT.md
@.planning/phases/03-translation-pipeline/03-RESEARCH.md
@.planning/phases/02-content-model/02-05-SUMMARY.md
@.planning/phases/03-translation-pipeline/03-01-SUMMARY.md
@.planning/phases/03-translation-pipeline/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the pre-push evidence pack</name>
  <files>.planning/phases/03-translation-pipeline/artifacts/deploy-gate.md</files>
  <read_first>
    - `.planning/phases/03-translation-pipeline/03-01-SUMMARY.md`: the lockfile delta and the green `npm ci --dry-run`
    - `.planning/phases/03-translation-pipeline/03-02-SUMMARY.md`: the schema field count move and the live query counts
    - `.planning/STATE.md`: the standing `npm run build` prohibition and the Phase 2 ordering invariant
    - `.planning/phases/03-translation-pipeline/03-RESEARCH.md` section "Package Legitimacy Audit": the three `too-new` SUS verdicts and why the evidence for them is this deploy rather than three separate ceremonial checkpoints
  </read_first>
  <action>
    Run, in order, and capture the exact output of each:
    `git status --short` (must show no unexpected staged content and no `.env` file staged)
    `git log --oneline origin/main..main | wc -l` and `git log --oneline origin/main..main | head -20`
    `npm ci --dry-run --ignore-scripts`
    `npx tsc --noEmit`
    `npx eslint`
    `npx next build`
    `node scripts/checks/route-smoke.mjs --verify` against a backgrounded `npx next start`
    `npx tsx scripts/checks/language-filter.check.ts`
    `npx tsx --env-file .env.vercel-prod scripts/checks/language-filter.check.ts --live`
    `npx tsx scripts/checks/translation.check.ts`

    Never run `npm run build`; the package script chains `prisma migrate deploy` and the Prisma CLI reads `.env`, which is the production database.

    Determine which git branch Vercel treats as the production branch rather than assuming it is `main`: run `npx vercel project inspect` or `npx vercel ls` if the CLI is authenticated, and if it is not, record that the answer must come from Saeid at the checkpoint. Record the finding either way.

    Confirm the production dataset currently holds zero Farsi documents by running a read-only count of `*[_type == "post" && language == "fa"]` under a raw-perspective client against `.env.vercel-prod`. This is the ordering-invariant baseline; it must be 0 before the deploy and it is what plan 03-10 will move off zero.

    Write `.planning/phases/03-translation-pipeline/artifacts/deploy-gate.md` with a PRE-PUSH section holding every command and its result, the commit count ahead of origin, the production branch finding, and the Farsi document count of 0. Paste no env value into the artifact.
  </action>
  <verify>
    <automated>npm ci --dry-run --ignore-scripts && npx tsc --noEmit && npx next build && npx tsx scripts/checks/language-filter.check.ts && npx tsx scripts/checks/translation.check.ts</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03-translation-pipeline/artifacts/deploy-gate.md` exists and contains a `## Pre-push` section
    - Every one of the ten commands above appears in that section with its captured result
    - The artifact records the commit count ahead of `origin/main` as a number
    - The artifact records the production Farsi document count as 0
    - The artifact records which branch Vercel deploys to production, or records explicitly that Saeid must confirm it at the checkpoint
    - `git status --short` output in the artifact shows no path beginning with `.env`
    - `npx next build` exits 0
    - `node scripts/checks/route-smoke.mjs --verify` reports 28/28
  </acceptance_criteria>
  <done>Everything Saeid needs in order to say yes or no is on one page, gathered from real command output rather than from claims.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Saeid authorises the push of main and the production deploy (D-11 step 2)</name>
  <what-built>
    The full local verification pack is assembled in `.planning/phases/03-translation-pipeline/artifacts/deploy-gate.md`: `npm ci --dry-run` is green for the first time (four transitive bumps, zero removals, no framework movement), `npx tsc --noEmit` is 0, `npx eslint` is clean, `npx next build` is 0, route smoke is 28/28, and both check scripts pass offline and live. Production currently holds zero Farsi documents. Nothing has been pushed and nothing has been deployed.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/03-translation-pipeline/artifacts/deploy-gate.md` and read the Pre-push section. Every command's real output is there.
    2. Confirm the commit count ahead of `origin/main` matches what you expect for the Phase 1 and Phase 2 work (roughly sixty commits).
    3. Confirm the production branch named in the artifact is the branch Vercel actually deploys to production. If the Vercel CLI could not answer, say which branch it is.
    4. Decide whether to authorise `git push origin <production branch>` now. This push will trigger a production deployment of the Phase 1 route-group move, the Phase 2 language filter and Studio split, and the Phase 3 `sourceUpdatedAt` schema field.
    5. This deploy is also the evidence pass for the three `too-new` package verdicts from the research audit (`@sanity/client` 7.26.2, `get-it` 8.8.3, `nanoid` 3.3.18). All three are already installed and already serving production traffic, are first-party repo-backed packages with over a million weekly downloads each, and carry no install script. `npx next build` and 28/28 route smoke are the meaningful evidence that the bumps did not regress anything. Confirm you accept that framing rather than wanting three separate registry walkthroughs.
  </how-to-verify>
  <resume-signal>Type "approved" plus the production branch name to authorise the push and deploy, or describe what you want changed first. Nothing is pushed until you answer.</resume-signal>
  <files>.planning/phases/03-translation-pipeline/artifacts/deploy-gate.md</files>
  <action>Present the Pre-push section of the evidence artifact and the verification questions above, then STOP and wait. Do not run `git push`, `vercel deploy` or any other command that touches remote state while this task is open. Record Saeid's exact reply, including the branch name he confirms, in the artifact under a `## Authorisation` heading before Task 3 begins. If he answers anything other than an explicit approval, halt the plan and report rather than proceeding.</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/03-translation-pipeline/artifacts/deploy-gate.md','utf8');process.exit(/## Authorisation/.test(s)?0:1)"</automated>
  </verify>
  <done>Saeid has explicitly authorised or declined the push, his exact wording is recorded in the artifact, and nothing has been pushed or deployed before that answer.</done>
</task>

<task type="auto">
  <name>Task 3: Push, deploy, and prove the filter and schema are live on production</name>
  <files>.planning/phases/03-translation-pipeline/artifacts/deploy-gate.md</files>
  <read_first>
    - `.planning/phases/03-translation-pipeline/artifacts/deploy-gate.md`: the Pre-push section and Saeid's answer
    - `src/sanity/lib/queries.ts`: the `EN_LANGUAGE` predicate whose liveness on production is what this task proves
  </read_first>
  <action>
    Only after the checkpoint returns an explicit approval, run `git push origin <branch Saeid named>`. Use no force flag, no `--no-verify` and no `--no-gpg-sign`. If the push is rejected, stop and report; do not resolve a divergence unilaterally.

    Wait for the production deployment to reach a ready state. Prefer `npx vercel ls` or `npx vercel inspect` if the CLI is authenticated; otherwise ask Saeid to confirm from the Vercel dashboard that the deployment finished and record which route was used. The regenerated production build is what makes the language filter live on the statically generated blog pages, so a deploy that is merely queued does not close this gate.

    Then prove liveness with three independent checks and record each:
    1. `npx tsx --env-file .env.vercel-prod scripts/checks/language-filter.check.ts --live` exits 0 against `blog_posts`.
    2. Fetch the live production `/blog` page and confirm it renders. Confirm the deployed commit SHA matches the pushed HEAD, using the Vercel deployment metadata or a build-id comparison.
    3. Open the production `/studio`, open any post, and confirm the Language radio, the Translation of reference, the read-only Translation Notes and the new Source Updated At field are present. The Source Updated At field is hidden on English documents by design, so confirm its absence there is the hidden rule firing rather than the field being missing, by checking that the schema deployed is the pushed one.

    Re-run the production Farsi document count and confirm it is still 0. The gate closes with production running the filter and holding zero Farsi documents, which is exactly the state plan 03-10 requires.

    Append a POST-DEPLOY section to `.planning/phases/03-translation-pipeline/artifacts/deploy-gate.md` recording the pushed SHA, the deployment id or URL, the three liveness results and the Farsi count. Update `.planning/STATE.md` to clear the `npm ci` blocker and to record that the deploy gate is closed.
  </action>
  <verify>
    <automated>npx tsx --env-file .env.vercel-prod scripts/checks/language-filter.check.ts --live</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline origin/main..main | wc -l` returns 0 after the push (or the equivalent for the branch Saeid named)
    - `.planning/phases/03-translation-pipeline/artifacts/deploy-gate.md` contains a `## Post-deploy` section recording the pushed SHA and the deployment id or URL
    - `npx tsx --env-file .env.vercel-prod scripts/checks/language-filter.check.ts --live` exits 0
    - The artifact records the production Farsi document count as 0 after the deploy
    - The artifact records the Studio confirmation for the four pipeline-related fields
    - `.planning/STATE.md` no longer lists the `npm ci` blocker as OPEN and records the deploy gate as closed
  </acceptance_criteria>
  <done>Production runs the Phase 2 filter and the 18-field schema, and holds zero Farsi documents. Plan 03-10 is unblocked; nothing before it may touch the production dataset with writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repository to origin | Roughly sixty unreviewed local commits cross into the shared remote and trigger a production deployment |
| lockfile to Vercel build | The repaired lockfile determines exactly which package versions production installs |
| production Studio to readers | A schema or filter regression is visible to every visitor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-11 | Tampering | npm supply chain, the three `too-new` SUS verdicts | high | mitigate | This deploy is the evidence pass. All three packages are already installed and already serving production traffic, are first-party and repo-backed with over a million weekly downloads, and carry no install script. `npx next build` plus 28/28 route smoke plus a live production `/blog` fetch is the verification, and it sits behind a blocking human gate that `workflow.auto_advance` cannot skip |
| T-03-21 | Elevation of Privilege | unauthorised push to the deploy branch | high | mitigate | The push happens only after an explicit typed approval naming the branch; no force flag, no `--no-verify`, no unilateral divergence resolution |
| T-03-22 | Tampering | production build not regenerated after the push | high | mitigate | Task 3 waits for a ready deployment and proves it three ways, including matching the deployed SHA to the pushed HEAD, because the statically generated blog pages only pick up the language filter on a rebuild |
| T-03-04 | Information Disclosure | secrets entering the push | high | mitigate | `git status --short` is captured pre-push and asserted to contain no `.env` path; `.env` and `.env.*` are gitignored |
| T-03-23 | Tampering | Phase 2 ordering invariant | critical | mitigate | The production Farsi document count is asserted to be 0 both before and after the deploy, so the filter is provably live before the first Farsi document can exist |
</threat_model>

<verification>
- `npx tsx --env-file .env.vercel-prod scripts/checks/language-filter.check.ts --live` exits 0 after the deploy
- The deployed commit SHA matches the pushed HEAD
- The production Farsi document count is 0
- `.planning/phases/03-translation-pipeline/artifacts/deploy-gate.md` has both a Pre-push and a Post-deploy section
- `.planning/STATE.md` records the gate as closed and the `npm ci` blocker as resolved
</verification>

<success_criteria>
D-11 step 2 is closed with Saeid's explicit authorisation. Production runs the Phase 2 English filter and the 18-field post schema, the production build has been regenerated, and the production dataset still holds zero Farsi documents. D-11 step 3 is now satisfiable.
</success_criteria>

## Artifacts this phase produces

Produced by this plan:

| Artifact | Kind | Notes |
|---|---|---|
| `.planning/phases/03-translation-pipeline/artifacts/deploy-gate.md` | new artifact | Pre-push evidence pack plus Post-deploy proof |
| production deployment of Phases 1 to 3 schema work | deployed state | Not a repo artifact; recorded by SHA and deployment id |
| `.planning/STATE.md` blocker clearance | modified planning state | `npm ci` blocker resolved, deploy gate recorded closed |

<output>
Create `.planning/phases/03-translation-pipeline/03-04-SUMMARY.md` when done. Record the pushed SHA, the deployment id, Saeid's exact approval wording, and the three liveness results.
</output>
