---
phase: 03-translation-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package-lock.json
  - scripts/lib/token-usage.ts
  - scripts/checks/env-preflight.check.ts
  - .planning/phases/03-translation-pipeline/artifacts/preflight.md
autonomous: true
requirements: [PIPE-01]

must_haves:
  truths:
    - "`npm ci --dry-run --ignore-scripts` exits 0 in the repo working tree (D-11 step 1)"
    - "The lockfile delta is confined to four transitive version bumps and six wasm shim additions; next, react, next-sanity and sanity did not move"
    - "One command reports, for each env file, whether ANTHROPIC_API_KEY / SANITY_API_TOKEN / DATABASE_URL are present, without printing any secret value"
    - "The same command reports whether the target Postgres has the TokenUsage table and at least one ADMIN user, resolving that ADMIN id"
  artifacts:
    - "package-lock.json (repaired, committed alone)"
    - "scripts/lib/token-usage.ts"
    - "scripts/checks/env-preflight.check.ts"
    - ".planning/phases/03-translation-pipeline/artifacts/preflight.md"
  key_links:
    - "resolveAdminUserId() is called before any paid token spend, so a missing ADMIN aborts a run instead of losing the spend record (RESEARCH Pitfall 6)"
    - "The repaired lockfile is what CI and Vercel install; it is verified by `npx next build`, never by `npm run build` (STATE.md standing blocker)"
---

<objective>
Close the two Wave-0 preconditions that everything else in this phase rests on: repair `package-lock.json` until `npm ci` is green (D-11 step 1), and resolve the three environment unknowns RESEARCH left open (A2, A3, A4) with a repeatable, secret-safe probe.

Purpose: `npm ci` is broken locally, in CI and on Vercel, which blocks the D-11 push-and-deploy gate. Separately, the pipeline cannot record `TokenUsage` without a real `User.id`, and RESEARCH could not confirm that key material and dev-DB parity exist. Both must be settled before any paid Batch API run.
Output: A green `npm ci --dry-run`, a reusable TokenUsage helper for CLI scripts, an env preflight check script, and a recorded preflight artifact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-translation-pipeline/03-CONTEXT.md
@.planning/phases/03-translation-pipeline/03-RESEARCH.md
@.planning/phases/03-translation-pipeline/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair package-lock.json until npm ci is green</name>
  <files>package-lock.json</files>
  <read_first>
    - `.planning/phases/03-translation-pipeline/03-RESEARCH.md` section "Wave 0: The `npm ci` Failure, Diagnosed and Repaired": the recorded STATE.md diagnosis is disproven there; the real failure is four transitive version mismatches, and the repair was executed and verified in an isolated copy
    - `.planning/STATE.md` Blockers/Concerns: carries the superseded diagnosis and the standing `npm run build` prohibition
    - `package.json`: confirm the 48 root dependency entries are unchanged before and after
  </read_first>
  <action>
    Run `npm install --package-lock-only` in the repo root. Do NOT run a bare `npm install`: the local `node_modules` came from `npm install --no-package-lock` and reifying it risks resurfacing the hybrid-install state plan 01-01 untangled. `--package-lock-only` rewrites only the lockfile.

    Then run `git diff --stat package-lock.json` and enumerate the version changes. The expected delta, measured in isolation on 2026-08-22, is exactly: `@sanity/client` 7.23.0 to 7.26.2, `@sanity/eventsource` 5.0.2 to 5.0.4, `get-it` 8.8.0 to 8.8.3, `nanoid` 3.3.11 to 3.3.18, plus six added entries under `@tailwindcss/oxide-wasm32-wasi/node_modules/` (`@emnapi/core`, `@emnapi/runtime`, `@emnapi/wasi-threads`, `@napi-rs/wasm-runtime`, `@tybys/wasm-util`, `tslib`), and zero removals. If `next`, `react`, `next-sanity`, `sanity`, `prisma` or `@anthropic-ai/sdk` moved, STOP and report: the registry state changed since research (assumption A7) and the delta needs reassessment before commit.

    Verify with `npm ci --dry-run --ignore-scripts`, which must print an `added N packages` line rather than `EUSAGE`. Expect and ignore the pre-existing `ERESOLVE overriding peer dependency` warning about `@auth/core` wanting `nodemailer@^6.8.0` against the root `^7.0.7` pin; it is a warning and does not fail the install.

    Then run the repo build gate: `npx tsc --noEmit` and `npx next build`. Never `npm run build`: the package script chains `prisma migrate deploy`, and the Prisma CLI reads `.env`, which points at the PRODUCTION database.

    Finally run `node scripts/checks/route-smoke.mjs --verify` against a backgrounded `npx next start`; the Phase 2 baseline is 28/28.

    Commit `package-lock.json` alone, with a message that names the four version bumps and the six wasm shim additions. Do not stage any other working-tree drift.
  </action>
  <acceptance_criteria>
    - `npm ci --dry-run --ignore-scripts` exits 0 and its output contains `added ` followed by a package count
    - `npm ci --dry-run --ignore-scripts` output does not contain `EUSAGE`
    - `git diff --numstat HEAD~1 -- package-lock.json` after the commit shows package-lock.json as the only changed path in that commit (`git show --stat --name-only HEAD` lists exactly one file)
    - `git show HEAD:package-lock.json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const l=JSON.parse(s).packages;for(const k of ['node_modules/next','node_modules/react','node_modules/next-sanity','node_modules/sanity','node_modules/@anthropic-ai/sdk'])console.log(k,l[k]&&l[k].version)})"` prints the same versions as `git show HEAD~1:package-lock.json` piped through the same script
    - `npx tsc --noEmit` exits 0
    - `npx next build` exits 0
    - `node scripts/checks/route-smoke.mjs --verify` reports 28/28
  </acceptance_criteria>
  <verify>
    <automated>npm ci --dry-run --ignore-scripts && npx tsc --noEmit && npx next build</automated>
  </verify>
  <done>`npm ci` is usable again locally, in CI and on Vercel; the lockfile change is a solo commit whose delta matches the researched four-bump table.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add scripts/lib/token-usage.ts: ADMIN resolution and awaited spend recording</name>
  <files>scripts/lib/token-usage.ts</files>
  <read_first>
    - `src/app/api/cv/design/route.ts` lines 300-354: the `createMany` shape and field mapping this helper copies
    - `src/app/api/cv/extract/route.ts` lines 180-254: the single-call field mapping; note its fire-and-forget `.catch`, which this helper deliberately does NOT copy
    - `src/lib/prisma.ts`: the client the helper imports; it reads `DATABASE_URL` at construction, which is why the `--env-file` choice controls the Postgres target
    - `prisma/schema.prisma`: the `TokenUsage` model and the `Role` enum (`ADMIN | AUTHOR | SUBSCRIBER`)
    - `.planning/phases/03-translation-pipeline/03-RESEARCH.md` section "TokenUsage Integration" and Pitfall 6
    - `.planning/phases/03-translation-pipeline/03-PATTERNS.md` section "TokenUsage recording inside scripts/translate-posts.ts"
  </read_first>
  <behavior>
    - `resolveAdminUserId()` returns the id of the oldest `ADMIN` user, or throws an Error whose message names the missing role and the fact that spend cannot be recorded
    - `recordTokenUsage()` awaits a `createMany` and returns the number of rows written
    - Passing an empty rows array to `recordTokenUsage()` writes nothing and returns 0 rather than throwing
    - Neither function logs a connection string, a token or any env value
  </behavior>
  <action>
    Create `scripts/lib/token-usage.ts` as a new directory `scripts/lib/`. Import `prisma` from `../../src/lib/prisma` by relative path; the scripts tree does not use the `@/` alias (see `scripts/checks/language-filter.check.ts` line 25).

    Export `resolveAdminUserId(): Promise<string>` implemented as `prisma.user.findFirst({ where: { role: "ADMIN" }, orderBy: { createdAt: "asc" }, select: { id: true } })`, throwing a message-rich Error when the result is null. The caller must invoke this at the top of a run, before any paid token spend, so that a missing ADMIN aborts before money is spent rather than after.

    Export `recordTokenUsage(userId: string, rows: Array<{ activity: string; model: string; inputTokens: number; outputTokens: number }>): Promise<number>` implemented as an awaited `prisma.tokenUsage.createMany({ data: rows.map(r => ({ userId, activity: r.activity, model: r.model, inputTokens: r.inputTokens, outputTokens: r.outputTokens, totalTokens: r.inputTokens + r.outputTokens })) })`. Return `result.count`. Short-circuit to 0 on an empty array. Do not attach a `.catch` and do not fire and forget: in a CLI a swallowed failure loses the spend record silently.

    Export a `TokenUsageRow` type for the row shape so callers get a checked object literal.

    Carry a header docblock in the Phase-2 house style that states why this file exists (no in-repo CLI has ever written `TokenUsage`; both existing writers take `userId` from an authenticated session and a script has none) and states the trap being avoided (a required foreign key discovered after a paid batch run).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint scripts/lib/token-usage.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/lib/token-usage.ts` exists and exports `resolveAdminUserId`, `recordTokenUsage` and `TokenUsageRow`
    - `grep -c "\.catch(" scripts/lib/token-usage.ts` returns 0
    - `grep -c "await prisma.tokenUsage.createMany" scripts/lib/token-usage.ts` returns 1
    - `grep -c "role: \"ADMIN\"" scripts/lib/token-usage.ts` returns 1
    - `npx tsc --noEmit` exits 0
    - `npx eslint scripts/lib/token-usage.ts` exits 0 with no output
  </acceptance_criteria>
  <done>A CLI script can resolve a real `User.id` and write awaited `TokenUsage` rows without inventing a script-local Prisma pattern.</done>
</task>

<task type="auto">
  <name>Task 3: Author and run scripts/checks/env-preflight.check.ts, record the artifact</name>
  <files>scripts/checks/env-preflight.check.ts, .planning/phases/03-translation-pipeline/artifacts/preflight.md</files>
  <read_first>
    - `scripts/checks/language-filter.check.ts` lines 1-60 and 479-560: the docblock, the `process.argv.slice(2)` flag form, the two-section offline/live split, and the "assert presence, never print the value" comment at the live client construction
    - `scripts/migrate-post-language.ts` lines 22-56: the no-fallback env reads and the loud header line
    - `.planning/phases/03-translation-pipeline/03-RESEARCH.md` section "Environment Availability" and the Assumptions Log rows A2, A3, A4
    - `scripts/lib/token-usage.ts` from Task 2
  </read_first>
  <action>
    Create `scripts/checks/env-preflight.check.ts`. It is a live-only check: it needs an `--env-file` and it touches Postgres, so it must be explicit about that in its docblock and in its run lines, exactly as `language-filter.check.ts` does.

    It must report, as booleans only and never as values, whether `ANTHROPIC_API_KEY`, `SANITY_API_TOKEN`, `NEXT_PUBLIC_SANITY_PROJECT_ID`, `NEXT_PUBLIC_SANITY_DATASET` and `DATABASE_URL` are defined in the loaded environment. Print the resolved `NEXT_PUBLIC_SANITY_DATASET` verbatim, because the dataset name is the operator read-back defence and is not a secret. Print no other env value, ever.

    When `DATABASE_URL` is defined, probe the database: call `resolveAdminUserId()` from `scripts/lib/token-usage.ts` and report whether it succeeded, and run `prisma.tokenUsage.count()` to prove the table exists. Catch a failure from either probe and report it as a named FAIL row rather than crashing the whole check, so one missing piece still yields a complete report.

    Use `node:assert` for the two hard requirements (`NEXT_PUBLIC_SANITY_DATASET` defined with no default, `NEXT_PUBLIC_SANITY_PROJECT_ID` defined) and report the rest as PASS/FAIL rows. End with a counted summary line and the terminating `env-preflight.check.ts: ALL PASS` convention when every row passed. Use the repo's script error convention: `main().catch((err: unknown) => { console.error(err); process.exit(1); });`

    Run it twice and capture the output:
    `npx tsx --env-file .env.local scripts/checks/env-preflight.check.ts`
    `npx tsx --env-file .env.vercel-prod scripts/checks/env-preflight.check.ts`

    Create `.planning/phases/03-translation-pipeline/artifacts/preflight.md` recording, per env file: the resolved dataset name, each presence boolean, the ADMIN-resolution outcome, and the `TokenUsage` row count. Record the answers to RESEARCH assumptions A2, A3 and A4 explicitly. If `ANTHROPIC_API_KEY` is absent from both files, record that as a BLOCKER row naming which env file it must be added to before plan 03-05 runs. If the dev database lacks `TokenUsage` or an ADMIN user, record the documented fallback: the dev rehearsal prints spend to stdout and to its run artifact, and success criterion 5 is proven on the production run instead. Never paste an env value into the artifact.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint scripts/checks/env-preflight.check.ts && npx tsx --env-file .env.local scripts/checks/env-preflight.check.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/checks/env-preflight.check.ts` exists and `npx tsx --env-file .env.local scripts/checks/env-preflight.check.ts` exits 0
    - The command output contains the literal `dataset=blog_posts_dev`
    - The command output contains no substring of any secret: verify by asserting the output contains neither `sk-ant` nor `postgres://` nor `postgresql://`
    - `grep -c "process.env.NEXT_PUBLIC_SANITY_DATASET ??" scripts/checks/env-preflight.check.ts` returns 0 (no silent dataset default)
    - `.planning/phases/03-translation-pipeline/artifacts/preflight.md` exists and contains one row each for `ANTHROPIC_API_KEY`, `DATABASE_URL`, `TokenUsage` and `ADMIN`, for both `.env.local` and `.env.vercel-prod`
    - `.planning/phases/03-translation-pipeline/artifacts/preflight.md` contains the strings `A2`, `A3` and `A4`
    - `npx eslint scripts/checks/env-preflight.check.ts` exits 0
  </acceptance_criteria>
  <done>RESEARCH assumptions A2, A3 and A4 are answered from live probes and recorded, with a written fallback for any gap, so no later plan discovers a missing key or a missing ADMIN mid-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to lockfile | Third-party package versions enter the build that CI and Vercel install |
| shell environment to script | Secrets (`ANTHROPIC_API_KEY`, `SANITY_API_TOKEN`, `DATABASE_URL`) cross into a process that prints to a terminal and writes artifacts |
| script to production Postgres | `.env` and `.env.vercel-prod` can resolve `DATABASE_URL` to the production database |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Tampering | `package-lock.json` repair | medium | mitigate | Task 1 pins the expected delta to four bumps plus six wasm shim additions and zero removals; a movement in `next`, `react`, `next-sanity`, `sanity`, `prisma` or `@anthropic-ai/sdk` stops the task before commit |
| T-03-04 | Information Disclosure | `env-preflight.check.ts` output and `preflight.md` | high | mitigate | Task 3 reports presence booleans only; the sole env value printed is the dataset name; acceptance criteria assert the output carries no `sk-ant` and no Postgres URL scheme; `.env*` stays gitignored and unstaged |
| T-03-08 | Tampering | `TokenUsage` foreign key | medium | mitigate | `resolveAdminUserId()` throws with a named message and is called before any spend; `recordTokenUsage` is awaited with no `.catch`, so a write failure is visible |
| T-03-11 | Tampering | npm supply chain, three `too-new` SUS verdicts (`@sanity/client`, `get-it`, `nanoid`) | high | mitigate | All three are already installed and already serving production traffic, are first-party repo-backed packages with 1M+ weekly downloads, and carry `postinstall: null`. Verification is evidence-backed rather than ceremonial: `npx next build` plus `route-smoke --verify` in Task 1, then the blocking human gate in plan 03-04 (D-11). No new package is installed by this phase. |
| T-03-15 | Tampering | `npm run build` reaching the production database | high | mitigate | Task 1 uses `npx next build` and states the reason inline; the package script chains `prisma migrate deploy` against `.env`, which is production |
</threat_model>

<verification>
- `npm ci --dry-run --ignore-scripts` exits 0 with an `added N packages` line
- `npx tsc --noEmit` exits 0
- `npx next build` exits 0
- `node scripts/checks/route-smoke.mjs --verify` reports 28/28
- `npx tsx --env-file .env.local scripts/checks/env-preflight.check.ts` exits 0
- `npx eslint scripts/lib/token-usage.ts scripts/checks/env-preflight.check.ts` exits 0
- `git show --name-only HEAD` for the lockfile commit lists exactly one path
</verification>

<success_criteria>
D-11 step 1 is closed: `npm ci` is green, so the push-and-deploy gate in plan 03-04 is unblocked. The three open environment questions from RESEARCH are answered from live probes and recorded, and a CLI script now has a supported way to resolve an ADMIN user and record spend.
</success_criteria>

## Artifacts this phase produces

Produced by this plan:

| Artifact | Kind | Notes |
|---|---|---|
| `package-lock.json` | modified config | Four transitive version bumps, six wasm shim additions, zero removals |
| `scripts/lib/token-usage.ts` | new file | New `scripts/lib/` directory |
| `resolveAdminUserId()` | exported function | Throws when no ADMIN user exists |
| `recordTokenUsage(userId, rows)` | exported function | Awaited `createMany`; returns row count |
| `TokenUsageRow` | exported type | `{ activity, model, inputTokens, outputTokens }` |
| `scripts/checks/env-preflight.check.ts` | new file | Live-only check, `--env-file` required |
| `.planning/phases/03-translation-pipeline/artifacts/preflight.md` | new artifact | Answers RESEARCH assumptions A2, A3, A4 |

<output>
Create `.planning/phases/03-translation-pipeline/03-01-SUMMARY.md` when done. Record the lockfile delta verbatim, the two preflight outputs (redacted to presence booleans), and the resolved answers to A2, A3 and A4.
</output>
