---
phase: 03-translation-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sanity/schemaTypes/postType.ts
  - src/sanity/lib/queries.ts
  - scripts/checks/language-filter.check.ts
autonomous: true
requirements: [PIPE-01]

must_haves:
  truths:
    - "A Farsi document can record which revision of its English source it was translated from, so staleness is exact and survives Saeid editing the draft (D-08)"
    - "The pipeline's post selection is expressed as GROQ in the one allowlisted home, reusing EN_LANGUAGE and STATUS_APPROVED rather than retyping either predicate"
    - "The sibling test is a count() subquery, so selection does not fetch every body just to filter client side"
    - "A slug supplied on the command line reaches GROQ as a $param, never as a template interpolation"
    - "The Phase 2 gate still passes: the postType field-count tripwire is updated deliberately, not bypassed"
  artifacts:
    - "src/sanity/schemaTypes/postType.ts with an 18th field, sourceUpdatedAt"
    - "src/sanity/lib/queries.ts with translationCandidatesQuery and translationStaleQuery"
    - "scripts/checks/language-filter.check.ts with EXPECTED_FIELD_COUNT at 18 and a new pipeline-query assertion section"
  key_links:
    - "EXPECTED_FIELD_COUNT moves 17 to 18 in the same task that adds sourceUpdatedAt, or the Phase 2 check fails (RESEARCH Pitfall 7)"
    - "ALLOWED_INTERPOLATIONS is closed to four identifiers; a new query may interpolate only EN_LANGUAGE and STATUS_APPROVED (RESEARCH Pitfall 8, assertion D)"
    - "queries.ts is already an allowlisted carrier of the text `language ==`, so the fa-side count subquery may live there and nowhere else under src/ (assertion H)"
---

<objective>
Add the two Sanity-side surfaces the pipeline reads and writes against: a `sourceUpdatedAt` field on `postType` that makes D-08 staleness exact, and the pipeline's post-selection GROQ in `src/sanity/lib/queries.ts`, the one place under `src/` allowed to carry the language predicate.

Purpose: the CLI in later waves must be able to ask "which approved English posts have no Farsi sibling" and "which siblings are stale" without inventing a second home for the language filter and without breaking the Phase 2 tripwires that were installed on purpose to catch exactly this kind of addition.
Output: one new schema field, two new exported queries, and the Phase 2 check script updated to cover both deliberately.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-translation-pipeline/03-CONTEXT.md
@.planning/phases/03-translation-pipeline/03-RESEARCH.md
@.planning/phases/03-translation-pipeline/03-PATTERNS.md
@.planning/phases/02-content-model/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sourceUpdatedAt to postType and move the field-count tripwire 17 to 18</name>
  <files>src/sanity/schemaTypes/postType.ts, scripts/checks/language-filter.check.ts</files>
  <read_first>
    - `src/sanity/schemaTypes/postType.ts` lines 113-172: the three Phase-2 fields (`language`, `translationOf`, `translationNotes`); `translationNotes` at lines 157-165 is the shape to copy for a pipeline-written field
    - `scripts/checks/language-filter.check.ts` lines 301-360: assertion I, `EXPECTED_FIELD_COUNT` at line 313 and the message at line 317 which explicitly demands the count be updated and the reason stated in the plan
    - `.planning/phases/03-translation-pipeline/03-RESEARCH.md` sections "Staleness detection (Claude's discretion, D-08)" and Pitfall 7
    - `.planning/phases/03-translation-pipeline/03-PATTERNS.md` section "src/sanity/schemaTypes/postType.ts (MODIFY: optional sourceUpdatedAt)"
  </read_first>
  <action>
    Add exactly one field to `postType.ts`, placed adjacent to `translationNotes` so the four pipeline-owned fields read as a group: `defineField({ name: "sourceUpdatedAt", title: "Source Updated At", type: "datetime", readOnly: true, hidden: ({ document }) => document?.language !== "fa", description: "The English source revision this translation was made from. Written by the translation script, not by hand." })`.

    This implements RESEARCH staleness option A, chosen over comparing `_updatedAt` values because Saeid reviewing and editing a Farsi draft bumps that draft's `_updatedAt`, which would make a stale translation look permanently fresh. Do not add a second `translatedAt` field; the draft's own `_createdAt` answers "when was this translated".

    The `hidden` predicate must not introduce the text `language ==` into the file in a form that changes assertion H's outcome. `postType.ts` is already an allowlisted carrier, so the assertion passes either way, but write the predicate as a TypeScript comparison (`document?.language !== "fa"`), not as GROQ.

    In the same task, change `EXPECTED_FIELD_COUNT` in `scripts/checks/language-filter.check.ts` from 17 to 18 and extend the comment above it to name `sourceUpdatedAt`, the plan that added it (03-02), and why. Do not delete or weaken the assertion: it is a deliberate anti-drift tripwire from plan 02-03 and its whole value is that it fires.

    Run `npx tsx scripts/checks/language-filter.check.ts` offline and `npx tsx --env-file .env.local scripts/checks/language-filter.check.ts --live` and confirm both still reach ALL PASS.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx tsx scripts/checks/language-filter.check.ts && npx eslint src/sanity/schemaTypes/postType.ts scripts/checks/language-filter.check.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "name: \"sourceUpdatedAt\"" src/sanity/schemaTypes/postType.ts` returns 1
    - `grep -c "EXPECTED_FIELD_COUNT = 18" scripts/checks/language-filter.check.ts` returns 1
    - `grep -c "EXPECTED_FIELD_COUNT = 17" scripts/checks/language-filter.check.ts` returns 0
    - `grep -c "sourceUpdatedAt" scripts/checks/language-filter.check.ts` is at least 1 (the reason is recorded next to the count)
    - `grep -c "name: \"translatedAt\"" src/sanity/schemaTypes/postType.ts` returns 0
    - `npx tsx scripts/checks/language-filter.check.ts` exits 0 and prints `language-filter.check.ts: ALL PASS`
    - `npx tsx --env-file .env.local scripts/checks/language-filter.check.ts --live` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>`postType` has 18 fields, the 18th is the pipeline-written source revision stamp, and the Phase 2 tripwire was updated deliberately with its reason recorded beside it.</done>
</task>

<task type="auto">
  <name>Task 2: Add the pipeline selection queries to src/sanity/lib/queries.ts</name>
  <files>src/sanity/lib/queries.ts</files>
  <read_first>
    - `src/sanity/lib/queries.ts` lines 1-45: the module docblock (server-only, four deliberate status variants that must not be unified) and the `EN_LANGUAGE` / `STATUS_APPROVED` fragment definitions
    - `scripts/checks/language-filter.check.ts` lines 38-50 (the nine-query `QUERIES` array), 116-140 (assertion B), 197-213 (assertion D, the closed interpolation allowlist), 272-300 (assertion H, the closed three-file text allowlist)
    - `.planning/phases/03-translation-pipeline/03-RESEARCH.md` section "The select query": the exact GROQ verified live against production on 2026-08-22
    - `.planning/phases/03-translation-pipeline/03-PATTERNS.md` section "src/sanity/lib/queries.ts (MODIFY: add the pipeline select query)"
  </read_first>
  <action>
    Add two exported constants to `src/sanity/lib/queries.ts`, below the existing nine, under a short section comment stating that these are the pipeline's SCRIPT-side reads and are deliberately not part of the public read path.

    `translationCandidatesQuery` selects approved English posts that have no Farsi sibling:
    filter on `_type == "post"`, `!(_id in path("drafts.**"))`, the interpolated `EN_LANGUAGE`, the interpolated `STATUS_APPROVED`, `(!defined($slug) || slug.current == $slug)`, and `count(*[_type == "post" && language == "fa" && translationOf._ref in [^._id, "drafts." + ^._id]]) == 0`.
    Project `_id`, `_updatedAt`, `title`, `description`, `metaDescription`, `"slug": slug.current`, `publishedAt`, `category`, `author`, `mainImage`, `body`.

    `translationStaleQuery` is the same filter with the count comparison inverted to `> 0`, projecting the same fields plus `"sibling": *[_type == "post" && language == "fa" && translationOf._ref in [^._id, "drafts." + ^._id]][0]{ _id, sourceUpdatedAt }`. This is the D-08 reporting surface: stale siblings are listed, never touched.

    Three hard constraints, each enforced by an existing assertion:
    1. Interpolate only `EN_LANGUAGE` and `STATUS_APPROVED`. Assertion D's `ALLOWED_INTERPOLATIONS` is a closed list of four identifier names, and every runtime value must stay a GROQ `$param`. The slug therefore arrives as `$slug`, never as a template expression.
    2. Do not retype either predicate literal; interpolate the exported constant, as every existing query does.
    3. Do not add these two constants to the check script's `QUERIES` array. That array is documented as the nine exported PUBLIC post queries and drives assertions B and C, whose expected counts encode CONTENT-02's public read contract. A script-side read is a different surface. Task 3 gives them their own assertion section instead.

    The `language == "fa"` text lands in `queries.ts`, which assertion H already allows. Do not create any other file under `src/` containing that text.

    Do not touch the existing nine queries or the four status-predicate constants.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx tsx scripts/checks/language-filter.check.ts && npx eslint src/sanity/lib/queries.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const translationCandidatesQuery" src/sanity/lib/queries.ts` returns 1
    - `grep -c "export const translationStaleQuery" src/sanity/lib/queries.ts` returns 1
    - `grep -c '\$slug' src/sanity/lib/queries.ts` is at least 2
    - `node -e "const q=require('fs').readFileSync('src/sanity/lib/queries.ts','utf8');const m=[...q.matchAll(/\\\$\{([^}]*)\}/g)].map(x=>x[1].trim());const bad=m.filter(x=>!['EN_LANGUAGE','STATUS_TOLERANT','STATUS_STRICT','STATUS_APPROVED'].includes(x));console.log(bad.join(','));process.exit(bad.length?1:0)"` exits 0
    - `npx tsx scripts/checks/language-filter.check.ts` exits 0 and prints `language-filter.check.ts: ALL PASS` (assertions B, C, D and H all still hold)
    - `grep -rl "language ==" src --include=*.ts --include=*.tsx | sort` lists exactly three paths: `src/sanity/lib/queries.ts`, `src/sanity/schemaTypes/postType.ts`, `src/sanity/structure.ts`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The pipeline's selection and staleness GROQ live in the one allowlisted module, reuse the shared fragments, and take the slug as a parameter.</done>
</task>

<task type="auto">
  <name>Task 3: Assert the pipeline queries in language-filter.check.ts and prove them live</name>
  <files>scripts/checks/language-filter.check.ts</files>
  <read_first>
    - `scripts/checks/language-filter.check.ts` lines 106-140 (assertion A and B style), 461-486 (assertion K and the offline summary line), 487-560 (the live section, its separately constructed clients and the raw-perspective client at 648-662)
    - `src/sanity/lib/queries.ts` as modified by Task 2
    - `.planning/phases/03-translation-pipeline/03-RESEARCH.md` sections "Perspective is the whole ballgame" and Pitfall 1
  </read_first>
  <action>
    Add an offline assertion section `L. Pipeline selection queries (PIPE-01)` after section K. Import `translationCandidatesQuery` and `translationStaleQuery` alongside the existing imports and assert, on the resolved strings:
    - each carries the `EN_LANGUAGE` predicate exactly once
    - each carries the `STATUS_APPROVED` predicate exactly once and neither carries `STATUS_TOLERANT` nor `STATUS_STRICT`
    - each references `$slug` and neither contains a `${` sequence outside the two allowed fragment names (already covered globally by assertion D, so here assert only that the string contains `$slug`)
    - each contains `path("drafts.**")`, so a draft English document can never be treated as a translation source
    - `translationCandidatesQuery` contains `) == 0` and `translationStaleQuery` contains `) > 0`, which is the only difference in their sibling test and the thing most likely to be copy-paste-broken
    Write each assertion message in the house style: expected versus actual plus the remedy.

    Extend the live section with `Live 4. Pipeline selection behaviour`. Build a client with `perspective: "raw"` (the live section already builds a raw client for its draft assertions at lines 648-662; follow the same one-client-per-perspective-with-a-comment rule rather than flipping an existing client). Run `translationCandidatesQuery` with `params: { slug: null }` and assert the returned count equals the count of approved English posts, since zero Farsi documents exist today. Run `translationStaleQuery` with the same params and assert it returns 0. Then run `translationCandidatesQuery` with `params: { slug: <the slug of the first returned candidate> }` and assert it returns exactly one row whose slug matches, which proves the `$slug` parameter path.

    Add a comment stating that the zero-count branch of the sibling subquery has not yet been exercised against real Farsi data and is proven in plan 03-09's dev rehearsal. Do not fabricate a Farsi document here; this check is read-only.

    Update the offline summary line's counts and keep the terminating ALL PASS convention.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx tsx scripts/checks/language-filter.check.ts && npx tsx --env-file .env.local scripts/checks/language-filter.check.ts --live</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "L. Pipeline selection queries" scripts/checks/language-filter.check.ts` returns 1
    - `grep -c "Live 4. Pipeline selection behaviour" scripts/checks/language-filter.check.ts` returns 1
    - `grep -c "translationCandidatesQuery" scripts/checks/language-filter.check.ts` is at least 3
    - `npx tsx scripts/checks/language-filter.check.ts` exits 0 and prints `language-filter.check.ts: ALL PASS`
    - `npx tsx --env-file .env.local scripts/checks/language-filter.check.ts --live` exits 0 and its output reports a candidate count of 11 for `blog_posts_dev` and a stale count of 0
    - `npx tsx --env-file .env.vercel-prod scripts/checks/language-filter.check.ts --live` exits 0 and its output reports a candidate count of 26 for `blog_posts` and a stale count of 0
    - `npx eslint scripts/checks/language-filter.check.ts` exits 0
  </acceptance_criteria>
  <done>Both pipeline queries are pinned offline against drift and proven to execute correctly, including the `$slug` parameter path, against both datasets read-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argument to GROQ | A `--slug` value supplied on the command line reaches the Content Lake query engine |
| script to Content Lake | A raw-perspective client can read every draft in the dataset |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Tampering | `translationCandidatesQuery` slug filter | high | mitigate | The slug is a GROQ `$param`, never a template interpolation. Assertion D's closed four-name `ALLOWED_INTERPOLATIONS` list enforces this mechanically; Task 3 adds a direct `$slug` assertion on top |
| T-03-16 | Tampering | schema field-count tripwire | medium | mitigate | `EXPECTED_FIELD_COUNT` moves 17 to 18 in the same task that adds the field, with the reason recorded next to the count; the assertion is never deleted or loosened |
| T-03-17 | Information Disclosure | raw-perspective client in the live check section | low | accept | The check is read-only and prints counts and slugs only. Slugs are public URLs. No token, no env value and no document body is printed |
| T-03-18 | Tampering | predicate spread under `src/` | medium | mitigate | Assertion H's closed three-file allowlist stays closed; the new queries live in the already-allowlisted `queries.ts` and no new carrier is created |
</threat_model>

<verification>
- `npx tsx scripts/checks/language-filter.check.ts` prints ALL PASS with `EXPECTED_FIELD_COUNT` at 18
- `npx tsx --env-file .env.local scripts/checks/language-filter.check.ts --live` exits 0
- `npx tsx --env-file .env.vercel-prod scripts/checks/language-filter.check.ts --live` exits 0
- `npx tsc --noEmit` exits 0
- `npx eslint src/sanity/schemaTypes/postType.ts src/sanity/lib/queries.ts scripts/checks/language-filter.check.ts` exits 0
- `grep -rl "language ==" src --include=*.ts --include=*.tsx` still lists exactly three files
</verification>

<success_criteria>
The Sanity read and write surfaces the pipeline needs exist: an 18th `postType` field recording the source revision, and two script-side selection queries in the single allowlisted GROQ module, both proven live against `blog_posts_dev` and `blog_posts` read-only. Every Phase 2 tripwire still fires and still passes.
</success_criteria>

## Artifacts this phase produces

Produced by this plan:

| Artifact | Kind | Notes |
|---|---|---|
| `sourceUpdatedAt` | new schema field on `postType` | datetime, readOnly, hidden unless the document is Farsi; takes the field count to 18 |
| `translationCandidatesQuery` | new export in `src/sanity/lib/queries.ts` | Approved EN posts with no Farsi sibling; takes `$slug` |
| `translationStaleQuery` | new export in `src/sanity/lib/queries.ts` | Approved EN posts whose Farsi sibling exists, projecting `sibling{_id, sourceUpdatedAt}` |
| `EXPECTED_FIELD_COUNT = 18` | modified constant | `scripts/checks/language-filter.check.ts` |
| assertion section `L` and `Live 4` | new check sections | `scripts/checks/language-filter.check.ts` |

<output>
Create `.planning/phases/03-translation-pipeline/03-02-SUMMARY.md` when done. Record the live candidate and stale counts for both datasets, and state explicitly why the two new queries were kept out of the check script's `QUERIES` array.
</output>
