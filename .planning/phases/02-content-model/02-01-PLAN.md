---
phase: 02-content-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sanity/lib/queries.ts
  - src/app/(en)/blog/page.tsx
  - src/app/(en)/page.tsx
  - src/app/(en)/blog/[categorySlug]/[postSlug]/page.tsx
  - src/app/(en)/authors/[slug]/page.tsx
  - src/app/sitemap.ts
  - src/app/api/v1/posts/route.ts
  - src/app/(en)/review/page.tsx
  - .planning/phases/02-content-model/artifacts/pre-extraction-ref.txt
autonomous: true
requirements: [CONTENT-02]

must_haves:
  truths:
    - "Every public English surface that reads posts gets its GROQ from src/sanity/lib/queries.ts, not from an inline literal (D-01)."
    - "All four status-predicate variants still exist after the move: tolerant, strict, approved-only, and none-at-all. Nothing was normalised."
    - "Each call site keeps its exact fetch shape: same generic, same params object, same Promise.all position, same route segment config."
    - "The English site behaves exactly as before: npx tsc --noEmit at 0, npx next build at 0, route smoke 28/28."
    - "Dashboards, api/dashboard routes and the publish-scheduled cron were not touched (D-02)."
  artifacts:
    - src/sanity/lib/queries.ts
    - .planning/phases/02-content-model/artifacts/pre-extraction-ref.txt
  key_links:
    - "blogIndexQuery imported by src/app/(en)/blog/page.tsx and used as the sole client.fetch argument there."
    - "homePageQuery imported by src/app/(en)/page.tsx as ONE compound constant, preserving the single round trip."
    - "postBySlugQuery, postStaticParamsQuery and postMetadataBySlugQuery imported by the post page, one per read site."
    - "postsByAuthorSlugQuery imported by the author page while authorQuery stays inline (it is not a post query)."
    - "sitemapQuery, postsByAuthorIdQuery and authorReviewPostsQuery imported by sitemap.ts, api/v1/posts GET and review/page.tsx."
---

<objective>
Step (1) of the roadmap's strict internal order: relocate every public post GROQ query into one shared module, byte for byte, with NO language filter yet.

This plan creates `src/sanity/lib/queries.ts` and repoints all eight public English surfaces named in D-01 to import from it. The language clause is deliberately absent here. It lands in plan 02-02, which is step (2). Splitting the two steps is what makes the refactor provable: the extraction must be a pure no-op, and plan 02-02's check script proves it by diffing the relocated text against this plan's recorded pre-extraction git ref.

Purpose: satisfy CONTENT-02's "single shared module" half and create the one place where the filter will later be expressed exactly once.
Output: `src/sanity/lib/queries.ts` with 3 exported status constants and 9 exported query constants, 7 repointed call sites, and a recorded pre-extraction git SHA.

**Interpretation recorded for roadmap success criterion 1** (per the phase brief): the criterion reads "all three blog page files render from queries imported from `src/sanity/lib/queries.ts`". `src/app/(en)/blog/[categorySlug]/page.tsx` is a ten-line `redirect()` to `/blog?cat=` and contains no GROQ at all, so it cannot import a query. The criterion is read as "every blog page file that fetches posts does so via the shared module", and that file is COVERED BY VACUITY. It is not modified by this phase and no query is invented for it.

**Interpretation recorded for `/review`** (research Open Question 1, D-01 default): `src/app/(en)/review/page.tsx` is treated as a public surface and routed through the shared module. It sits under `(en)`, its chrome is English, and Farsi review happens in the Studio per the design. The change is inert today. Flipping it later is a one-line edit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-model/02-CONTEXT.md
@.planning/phases/02-content-model/02-PATTERNS.md
@.planning/phases/02-content-model/02-RESEARCH.md
@.planning/phases/01-route-groups/01-02-SUMMARY.md
</context>

<hard_constraints>
1. **NEVER run `npm run build`.** It chains `prisma migrate deploy` against the PRODUCTION database. The build gate is `npx next build`.
2. **Run no package-manager install command of any kind.** `npm ci` is broken at HEAD (pre-existing, out of scope) and `node_modules` is already correct. `package.json` and `package-lock.json` must be byte-unchanged at the end of this plan.
3. **Do not add or change a dependency.** This phase needs none.
4. **Do not touch** `src/app/(en)/dashboard/**`, `src/app/api/dashboard/**`, or `src/app/api/cron/publish-scheduled/route.ts`. They stay unfiltered by decision D-02.
5. **Do not normalise the status predicates.** Four distinct variants exist. All four must survive character for character. This is the top named risk of the phase.
6. **Do not fix the missing status filter in `sitemap.ts`.** It is a real pre-existing SEO bug and it is explicitly out of scope; fixing it would destroy the provably-inert property this plan depends on.
7. **No `defineQuery`, no `groq` tag, no typegen generic.** No such construct exists anywhere in this repo. Plain untagged template literals only.
8. **No AI attribution in commits.** No `Co-Authored-By`, no `Claude-Session`, no "Generated with" line, ever.
9. `npx tsc --noEmit` must exit 0 after every task. The lint gate is ON.
10. If `npx next build` fails on a database connection, the preview Supabase project has paused on idle. Wake it in the Supabase dashboard and re-run. Do not work around it in code.
</hard_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Record the pre-extraction ref and create the query module with the blog index and homepage queries</name>

  <files>
    .planning/phases/02-content-model/artifacts/pre-extraction-ref.txt,
    src/sanity/lib/queries.ts,
    src/app/(en)/blog/page.tsx,
    src/app/(en)/page.tsx
  </files>

  <read_first>
    - `src/app/(en)/blog/page.tsx` (the `const query` block at line 28 and the `client.fetch(query)` call at line 53 inside `Promise.all`)
    - `src/app/(en)/page.tsx` (the `const query` block at line 24, `export const revalidate = 30` at line 44, the `client.fetch(query)` call at line 75)
    - `src/sanity/env.ts` (the repo's only module-of-exported-constants under `src/sanity/`, whose conventions `queries.ts` mirrors: no "use client", no side effects, named exports only, no default export)
    - `src/sanity/lib/client.ts` (confirms `apiVersion` 2025-10-07, which is why draft suppression is already free and no draft clause may be added)
    - `.planning/phases/02-content-model/02-PATTERNS.md` sections "`src/sanity/lib/queries.ts` (NEW)", "`src/app/(en)/blog/page.tsx`" and "`src/app/(en)/page.tsx`" (verbatim query text and the fetch patterns to preserve)
    - `.planning/phases/02-content-model/02-RESEARCH.md` section "Complete query inventory (the byte-faithful contract)" (rows 1 through 4)
  </read_first>

  <action>
Step A, before editing any source file. Create `.planning/phases/02-content-model/artifacts/` if it does not exist, run `git rev-parse HEAD`, and write the resulting 40-character SHA plus a trailing newline into `.planning/phases/02-content-model/artifacts/pre-extraction-ref.txt`. Nothing else goes in that file. Plan 02-02's check script reads this ref and uses `git show <ref>:<path>` to prove each relocated query is textually identical to the query it replaced, so capturing it AFTER an edit would silently void the phase's strongest anti-drift gate.

Step B. Create `src/sanity/lib/queries.ts`. Module conventions, taken from `src/sanity/env.ts`: no `"use client"` directive, no imports, no side effects, named exports only, no default export, double-quoted strings, 2-space indent, trailing commas. Open the file with a short header comment stating that it is the only home for public post GROQ, that it is server-only and must never be imported by a `"use client"` component, and that the four status-predicate variants below are deliberate carry-overs that must not be unified.

Step C. Export three status-predicate constants holding the exact predicate text lifted from the call sites. Name them `STATUS_TOLERANT`, `STATUS_STRICT` and `STATUS_APPROVED`. `STATUS_TOLERANT` is the three-branch predicate used today by the blog index and the author page (approved, or status undefined, or scheduled with `publishedAt` already past). `STATUS_STRICT` is the two-branch predicate used today by the homepage (approved, or scheduled with `publishedAt` already past, with no undefined-status branch). `STATUS_APPROVED` is the bare equality used today by the post page. Copy all three character for character from 02-PATTERNS.md, including the parentheses, the spacing around `||` and `&&`, and the `publishedAt <= now()` form. Export them rather than keeping them module-private (a deliberate deviation from the research snippet) so that plan 02-02's check script can assert, per query, which variant is present. That assertion is the mechanical guard against accidental normalisation.

Step D. Export `blogIndexQuery`: the entire compound object query currently at `src/app/(en)/blog/page.tsx` line 28, as one constant. Substitute `${STATUS_TOLERANT}` for the inline status predicate inside the `posts` branch and preserve every other character, including the `categories` branch, the `authors` branch, the `order(featured desc, publishedAt desc)` clause, the `"bodyExcerpt": pt::text(body)[0...300]` projection and the `[0...6]` slice on authors. The `categories` and `authors` branches are not post queries and get no treatment in this plan or the next.

Step E. Export `homePageQuery`: the entire compound object query currently at `src/app/(en)/page.tsx` line 24, as ONE constant covering all three branches (`heroPosts`, `latestPosts`, `categories`). Do NOT split it into three constants. The call site issues a single `client.fetch`, and splitting would turn one network round trip into three, which is a behaviour change. Substitute `${STATUS_STRICT}` at all FOUR places that predicate appears: once in `heroPosts`, once in `latestPosts`, and twice in the `categories` branch, where it sits inside `count(*[... references(^._id)])` used both as the category filter and as the projected `postCount`. The two nested occurrences are the easiest to miss in this phase; count them before moving on.

Step F. Repoint `src/app/(en)/blog/page.tsx`. Delete the module-level `const query` block entirely. Add `import { blogIndexQuery } from "@/sanity/lib/queries";` alongside the existing `import { client } from "@/sanity/lib/client";`. Change the fetch argument at line 53 from `query` to `blogIndexQuery`. Leave the call untyped: it is `client.fetch(query)` with no generic today, and adding one would change the inferred type of `data` and risk new type errors. Leave its position inside `Promise.all` next to the Prisma `postView.groupBy` call, and leave the `data.categories ?? []` style access alone.

Step G. Repoint `src/app/(en)/page.tsx` the same way: delete `const query`, add `import { homePageQuery } from "@/sanity/lib/queries";`, change the argument at line 75 to `homePageQuery`. Leave `export const revalidate = 30;` exactly where it is.

Nothing else changes in either page. No component, no props, no rendering, no route segment config.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>node -e 'const fs=require("fs");const q=fs.readFileSync("src/sanity/lib/queries.ts","utf8");const need=["export const STATUS_TOLERANT","export const STATUS_STRICT","export const STATUS_APPROVED","export const blogIndexQuery","export const homePageQuery"];for(const n of need){if(!q.includes(n)){console.error("MISSING export: "+n);process.exit(1);}}const strict=(q.match(/\$\{STATUS_STRICT\}/g)||[]).length;if(strict!==4){console.error("STATUS_STRICT interpolations: "+strict+" (expected 4)");process.exit(1);}const tol=(q.match(/\$\{STATUS_TOLERANT\}/g)||[]).length;if(tol!==1){console.error("STATUS_TOLERANT interpolations: "+tol+" (expected 1 at this task)");process.exit(1);}console.log("queries.ts task-1 shape OK: 5 exports, STATUS_STRICT x4, STATUS_TOLERANT x1");'</automated>
    <automated>node -e 'const fs=require("fs");const files=["src/app/(en)/blog/page.tsx","src/app/(en)/page.tsx"];for(const f of files){const s=fs.readFileSync(f,"utf8");if(s.includes("_type == \"post\"")){console.error("inline post GROQ remains in "+f);process.exit(1);}if(!s.includes("@/sanity/lib/queries")){console.error("no shared-module import in "+f);process.exit(1);}}console.log("blog index + homepage repointed OK");'</automated>
    <automated>node -e 'const {execFileSync}=require("child_process");const ref=require("fs").readFileSync(".planning/phases/02-content-model/artifacts/pre-extraction-ref.txt","utf8").trim();if(!/^[0-9a-f]{40}$/.test(ref)){console.error("pre-extraction-ref.txt is not a 40-char SHA: "+ref);process.exit(1);}execFileSync("git",["cat-file","-e",ref+"^{commit}"]);console.log("pre-extraction ref resolves: "+ref);'</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/02-content-model/artifacts/pre-extraction-ref.txt` contains exactly one 40-character lowercase hex SHA and `git cat-file -e <sha>^{commit}` exits 0.
    - That SHA names a commit whose tree still contains the inline query text, i.e. it was captured before Step B. Proof: `git show <sha>:"src/app/(en)/blog/page.tsx"` contains the string `const query = ` .
    - `src/sanity/lib/queries.ts` exists, contains no `"use client"` directive, no `import` statement, and no `export default`.
    - `src/sanity/lib/queries.ts` exports exactly these five symbols at this task: `STATUS_TOLERANT`, `STATUS_STRICT`, `STATUS_APPROVED`, `blogIndexQuery`, `homePageQuery`.
    - `${STATUS_STRICT}` appears exactly 4 times in `src/sanity/lib/queries.ts`; `${STATUS_TOLERANT}` exactly 1 time; `${STATUS_APPROVED}` 0 times (its queries land in Task 2).
    - The source text of `src/sanity/lib/queries.ts` contains neither `defineQuery` nor a `groq` tag.
    - `src/app/(en)/blog/page.tsx` and `src/app/(en)/page.tsx` each import from `@/sanity/lib/queries` and each contain zero post-type GROQ text.
    - `src/app/(en)/page.tsx` still contains exactly one `client.fetch(` call (the compound query was not split).
    - `export const revalidate = 30;` is still present in `src/app/(en)/page.tsx`.
    - `npx tsc --noEmit` exits 0.
    - `git status --porcelain package.json package-lock.json` prints nothing.
  </acceptance_criteria>

  <done>
    The shared module exists with the three status constants and the two compound queries, the blog index and homepage fetch through it, the pre-extraction SHA is recorded and resolvable, and the typecheck is clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract the post page's three queries and the author page's post query</name>

  <files>
    src/sanity/lib/queries.ts,
    src/app/(en)/blog/[categorySlug]/[postSlug]/page.tsx,
    src/app/(en)/authors/[slug]/page.tsx
  </files>

  <read_first>
    - `src/app/(en)/blog/[categorySlug]/[postSlug]/page.tsx` (the `const postQuery` block at line 14, the inline literal inside `client.fetch<{ categorySlug: string; slug: string }[]>(...)` at line 37, the inline literal inside `client.fetch(...)` at line 53, the `client.fetch(postQuery, { slug: postSlug })` call at line 113, and `export const revalidate = 3600` at line 12)
    - `src/app/(en)/authors/[slug]/page.tsx` (the `const authorQuery` block at line 10, the `const postsQuery` block at line 27, the two fetches at lines 95 and 96 inside `Promise.all`, and `export const revalidate = 60` at line 85)
    - `src/sanity/lib/queries.ts` (current state after Task 1, so the new constants are appended in the same style)
    - `.planning/phases/02-content-model/02-PATTERNS.md` sections "`src/app/(en)/blog/[categorySlug]/[postSlug]/page.tsx`" and "`src/app/(en)/authors/[slug]/page.tsx`" (verbatim query text, the typed-fetch pattern, the param pattern)
    - `.planning/phases/02-content-model/02-RESEARCH.md` section "Complete query inventory" (rows 5 through 8) and "Pitfall 1: The shared slug makes `[0]` non-deterministic"
  </read_first>

  <action>
Append four constants to `src/sanity/lib/queries.ts`, in the order below, matching the file's existing style.

`postBySlugQuery`: the query currently declared as `const postQuery` at line 14 of the post page. Substitute `${STATUS_APPROVED}` for the inline `status == "approved"` clause. Keep the `slug.current == $slug` clause as a GROQ parameter, keep the trailing `[0]` single-document selector, and keep the entire projection including the body, the author join and the category join. Research flags this as the highest-risk query in the phase, because Farsi siblings will reuse the English slug verbatim and `[0]` would then pick a document by GROQ's default ordering rather than by intent. It gets the language clause in plan 02-02 with the same rigour as the listings.

`postStaticParamsQuery`: the inline literal currently passed to `client.fetch<{ categorySlug: string; slug: string }[]>(...)` at line 37, inside `generateStaticParams`. Substitute `${STATUS_APPROVED}`. Keep both `defined(slug.current)` and `defined(category->slug.current)` guards and the `"categorySlug": category->slug.current` projection. Keep the generic on the call site; do not move typing into `queries.ts`.

`postMetadataBySlugQuery`: the inline literal currently passed to `client.fetch(...)` at line 53, inside `generateMetadata`. This query carries NO status predicate at all today. Carry that over exactly: no `${STATUS_...}` interpolation goes into this constant. Do not add one, and do not "fix" the asymmetry with the body query. The phase changes language semantics only.

`postsByAuthorSlugQuery`: the query currently declared as `const postsQuery` at line 27 of the author page. Substitute `${STATUS_TOLERANT}`. Keep the `author->slug.current == $slug` clause, the `order(publishedAt desc)` clause, the `[0...20]` slice, and the `mainImage { asset->{ url } }` projection shape (note it differs from the `"mainImage": mainImage.asset->url` form used elsewhere; do not unify them).

Then repoint the two pages.

In the post page: delete the `const postQuery` block; replace the two inline literals at lines 37 and 53 with the imported constants; add one import line for `postBySlugQuery`, `postStaticParamsQuery` and `postMetadataBySlugQuery` from `@/sanity/lib/queries`. Preserve every call shape exactly: the generic `client.fetch<{ categorySlug: string; slug: string }[]>(postStaticParamsQuery)`, the params object `{ slug: postSlug }` on both parameterised fetches, and `export const revalidate = 3600;`.

In the author page: delete the `const postsQuery` block and add `import { postsByAuthorSlugQuery } from "@/sanity/lib/queries";`. Leave `const authorQuery` exactly where it is, untouched and un-relocated. It queries the author document type, not posts, and this module is for post queries only. Update only the second fetch inside `Promise.all` (the one at line 96) to take `postsByAuthorSlugQuery`; the `authorQuery` fetches at lines 53 and 95 keep their current argument. Leave `export const revalidate = 60;` alone.

Do not touch `src/app/(en)/authors/page.tsx`; it fetches only author documents.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>node -e 'const fs=require("fs");const q=fs.readFileSync("src/sanity/lib/queries.ts","utf8");const need=["export const postBySlugQuery","export const postStaticParamsQuery","export const postMetadataBySlugQuery","export const postsByAuthorSlugQuery"];for(const n of need){if(!q.includes(n)){console.error("MISSING export: "+n);process.exit(1);}}const c=(re)=>(q.match(re)||[]).length;if(c(/\$\{STATUS_APPROVED\}/g)!==2){console.error("STATUS_APPROVED interpolations: "+c(/\$\{STATUS_APPROVED\}/g)+" (expected 2)");process.exit(1);}if(c(/\$\{STATUS_TOLERANT\}/g)!==2){console.error("STATUS_TOLERANT interpolations: "+c(/\$\{STATUS_TOLERANT\}/g)+" (expected 2)");process.exit(1);}if(c(/\$\{STATUS_STRICT\}/g)!==4){console.error("STATUS_STRICT interpolations changed: "+c(/\$\{STATUS_STRICT\}/g)+" (expected 4)");process.exit(1);}console.log("queries.ts task-2 shape OK: 9 exports so far, TOLERANT x2 STRICT x4 APPROVED x2");'</automated>
    <automated>node -e 'const fs=require("fs");const files=["src/app/(en)/blog/[categorySlug]/[postSlug]/page.tsx","src/app/(en)/authors/[slug]/page.tsx"];for(const f of files){const s=fs.readFileSync(f,"utf8");if(s.includes("_type == \"post\"")){console.error("inline post GROQ remains in "+f);process.exit(1);}if(!s.includes("@/sanity/lib/queries")){console.error("no shared-module import in "+f);process.exit(1);}}const a=fs.readFileSync("src/app/(en)/authors/[slug]/page.tsx","utf8");if(!a.includes("const authorQuery")){console.error("authorQuery must stay inline in the author page");process.exit(1);}console.log("post page + author page repointed OK, authorQuery preserved inline");'</automated>
  </verify>

  <acceptance_criteria>
    - `src/sanity/lib/queries.ts` exports nine query constants in total after this task: `blogIndexQuery`, `homePageQuery`, `postBySlugQuery`, `postStaticParamsQuery`, `postMetadataBySlugQuery`, `postsByAuthorSlugQuery`, plus the three status constants. (`sitemapQuery`, `postsByAuthorIdQuery` and `authorReviewPostsQuery` arrive in Task 3.)
    - Interpolation counts in `src/sanity/lib/queries.ts` are exactly: `${STATUS_TOLERANT}` 2, `${STATUS_STRICT}` 4, `${STATUS_APPROVED}` 2. Total 8. Three of the nine queries carry no status predicate by design.
    - The body of `postMetadataBySlugQuery` contains no `${STATUS_` substring.
    - `postBySlugQuery` still ends with the single-document `[0]` selector and still references `$slug`.
    - `postStaticParamsQuery` still contains both `defined(` guards.
    - `src/app/(en)/blog/[categorySlug]/[postSlug]/page.tsx` contains zero post-type GROQ text, still declares `export const revalidate = 3600;`, and still passes `{ slug: postSlug }` to both parameterised fetches.
    - `src/app/(en)/authors/[slug]/page.tsx` still declares `const authorQuery` inline and still declares `export const revalidate = 60;`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>

  <done>
    The post page's three reads and the author page's post read all resolve through the shared module, `authorQuery` stayed put, and all four status-predicate variants are still distinguishable in `queries.ts`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extract the sitemap, api/v1/posts GET and review-page queries, then run the full regression gate</name>

  <files>
    src/sanity/lib/queries.ts,
    src/app/sitemap.ts,
    src/app/api/v1/posts/route.ts,
    src/app/(en)/review/page.tsx
  </files>

  <read_first>
    - `src/app/sitemap.ts` (the typed inline `client.fetch<{ posts: PostItem[]; authors: AuthorItem[] }>(...)` at line 23 and the Prisma try/catch graceful-degradation block below it)
    - `src/app/api/v1/posts/route.ts` (the GET read at line 52 with its `SanityPostListItem[]` generic and its two params `authorId` and `siteUrl`; also note the POST handler further down, which this plan does NOT touch)
    - `src/app/(en)/review/page.tsx` (the read at line 71 and `export const dynamic = "force-dynamic";` at line 7)
    - `src/sanity/lib/queries.ts` (current state after Task 2)
    - `.planning/phases/02-content-model/02-PATTERNS.md` sections "`src/app/sitemap.ts`" and "`src/app/api/v1/posts/route.ts` GET + `src/app/(en)/review/page.tsx`"
    - `.planning/phases/02-content-model/02-RESEARCH.md` section "Complete query inventory" (rows 9 through 11) and "Pitfall 10"
    - `scripts/checks/route-smoke.mjs` (header block: it needs a server on `GATE_BASE_URL`, default `http://localhost:3000`, and verifies against the phase-01 baseline)
  </read_first>

  <action>
Append three constants to `src/sanity/lib/queries.ts`.

`sitemapQuery`: the entire compound object literal currently passed to the typed fetch at line 23 of `src/app/sitemap.ts`, covering both the `posts` and `authors` branches, as ONE constant. Preserve its multi-line `*[ ... ]` formatting verbatim; it differs from the one-line form used elsewhere in the inventory and that difference is not to be tidied. This query carries NO status predicate today. Do not add one. Keep the `defined(slug.current)`, `defined(category->slug.current)` and `category->active == true` clauses exactly as they are.

`postsByAuthorIdQuery`: the inline literal currently passed to the GET fetch at line 52 of `src/app/api/v1/posts/route.ts`. No status predicate today; do not add one. Keep the `author._ref == $authorId` clause, the `order(coalesce(publishedAt, _createdAt) desc)` clause, and the full projection including `"url": select(defined(category) => $siteUrl + "/blog/" + category->slug.current + "/" + slug.current, null)` and the `body[]{ ... }` projection.

`authorReviewPostsQuery`: the inline literal currently passed to the fetch at line 71 of `src/app/(en)/review/page.tsx`. No status predicate today; do not add one. Do NOT merge this with `postsByAuthorIdQuery` even though the filters look identical: this one has no `url` projection, projects `"slug": slug.current`, and takes only `$authorId`. Merging would change at least one caller's output shape. Duplicated projection text between these two is acceptable and intended. The "exactly one place" criterion in CONTENT-02 is about the language filter, not about projections.

Repoint the three call sites. In each, replace the inline template-literal argument with the imported constant and add the import from `@/sanity/lib/queries`. Preserve every generic and params object: `client.fetch<{ posts: PostItem[]; authors: AuthorItem[] }>(sitemapQuery)` in the sitemap, `client.fetch<SanityPostListItem[]>(postsByAuthorIdQuery, { authorId: ..., siteUrl: ... })` in the API route, and `client.fetch<SanityPostListItem[]>(authorReviewPostsQuery, { authorId: sanityAuthorId })` in the review page. Leave the sitemap's Prisma try/catch block, the API route's auth and API-key handling, and `export const dynamic = "force-dynamic";` untouched.

In `src/app/api/v1/posts/route.ts`, change the GET read only. Do not open or edit the POST handler in this plan; the `language: "en"` stamp required by D-04 lands in plan 02-03, in a different wave, to keep this plan's diff a pure read-path refactor.

Then run the full regression gate for this plan. Typecheck, then `npx next build` (never `npm run build`), then start the built app with `npx next start` in a background shell and run `node scripts/checks/route-smoke.mjs --verify` against it, expecting 28/28 and `ALL PASS`. Stop the background server afterwards. Note that route smoke discovers a real post href from the served `/blog` HTML and real guide URLs from `/sitemap.xml`, so an extraction that silently emptied either surface would fail discovery loudly rather than pass quietly.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>node -e 'const fs=require("fs");const q=fs.readFileSync("src/sanity/lib/queries.ts","utf8");const need=["export const sitemapQuery","export const postsByAuthorIdQuery","export const authorReviewPostsQuery"];for(const n of need){if(!q.includes(n)){console.error("MISSING export: "+n);process.exit(1);}}const c=(re)=>(q.match(re)||[]).length;const t=c(/\$\{STATUS_TOLERANT\}/g),s=c(/\$\{STATUS_STRICT\}/g),a=c(/\$\{STATUS_APPROVED\}/g);if(t!==2||s!==4||a!==2){console.error("status interpolation drift: TOLERANT="+t+" STRICT="+s+" APPROVED="+a+" (expected 2/4/2)");process.exit(1);}console.log("queries.ts final shape OK: 9 queries, status interpolations 2/4/2");'</automated>
    <automated>node -e 'const fs=require("fs");const files=["src/app/(en)/blog/page.tsx","src/app/(en)/page.tsx","src/app/(en)/blog/[categorySlug]/[postSlug]/page.tsx","src/app/(en)/authors/[slug]/page.tsx","src/app/sitemap.ts","src/app/api/v1/posts/route.ts","src/app/(en)/review/page.tsx"];let bad=0;for(const f of files){const s=fs.readFileSync(f,"utf8");if(s.includes("_type == \"post\"")){console.error("inline post GROQ remains in "+f);bad++;}if(!s.includes("@/sanity/lib/queries")){console.error("no shared-module import in "+f);bad++;}}if(bad){process.exit(1);}console.log("all 7 public call sites repointed, zero inline post GROQ");'</automated>
    <automated>npx next build</automated>
    <automated>node scripts/checks/route-smoke.mjs --verify</automated>
    <automated>node -e 'const {execSync}=require("child_process");const out=execSync("git status --porcelain package.json package-lock.json").toString().trim();if(out){console.error("package manifest changed: "+out);process.exit(1);}console.log("package.json and package-lock.json unchanged");'</automated>
  </verify>

  <acceptance_criteria>
    - `src/sanity/lib/queries.ts` exports exactly 9 query constants and 3 status constants, 12 named exports total, and still has no `EN_LANGUAGE` export (that is plan 02-02's work).
    - Status interpolation counts in `src/sanity/lib/queries.ts` are still exactly `${STATUS_TOLERANT}` 2, `${STATUS_STRICT}` 4, `${STATUS_APPROVED}` 2.
    - `sitemapQuery`, `postsByAuthorIdQuery` and `authorReviewPostsQuery` each contain no `${STATUS_` substring.
    - `postsByAuthorIdQuery` still contains `"url": select(` and still references `$siteUrl`; `authorReviewPostsQuery` contains neither.
    - All seven public call-site files import from `@/sanity/lib/queries` and contain zero post-type GROQ text.
    - `src/app/(en)/blog/[categorySlug]/page.tsx` is unmodified (`git status --porcelain` does not list it). It is the redirect file covered by vacuity.
    - `git diff --name-only` lists no file under `src/app/(en)/dashboard/`, `src/app/api/dashboard/` or `src/app/api/cron/`.
    - `npx tsc --noEmit` exits 0.
    - `npx next build` exits 0 and `npm run build` was never invoked.
    - `node scripts/checks/route-smoke.mjs --verify` against `npx next start` reports 28/28 and prints `ALL PASS`.
    - `git status --porcelain package.json package-lock.json` prints nothing.
  </acceptance_criteria>

  <done>
    All eight D-01 surfaces are accounted for (seven repointed, one covered by vacuity), the shared module holds every public post query, and the English site is proven unchanged by a green build plus a 28/28 route smoke.
  </done>
</task>

</tasks>

<!-- planner-discipline-allow: _type == "post" -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public HTTP to Next.js server components | Untrusted route params (`$slug`, `$categorySlug`) cross here and reach GROQ |
| public HTTP to `/api/v1/posts` | API-key-authenticated caller; `$authorId` derives from the authenticated principal, `$siteUrl` from server env |
| Next.js server to Sanity Content Lake | Query text authored in this repo executes against the dataset with a read token |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering / Information disclosure | `src/sanity/lib/queries.ts` GROQ text | high | mitigate | Every runtime value stays a GROQ `$param` (`$slug`, `$authorId`, `$siteUrl`). The only interpolations permitted in the module are its own module-level constants. Task acceptance criteria pin the exact interpolation counts, and plan 02-02's check script asserts no other `${` appears in any exported query. |
| T-02-02 | Tampering | Status-predicate semantics during relocation | high | mitigate | Four variants preserved as three named constants plus an explicit no-predicate case. Per-task counts (2/4/2) are asserted mechanically, and plan 02-02 diffs each relocated query against the pre-extraction git ref recorded in Task 1. |
| T-02-03 | Elevation of privilege | D-02 surfaces left unfiltered (dashboards, api/dashboard, cron) | medium | accept | Deliberate per D-02: admins manage both languages. Every one of those surfaces sits behind `auth()`, `authenticateApiKey` or the cron secret. This plan does not modify any of them, proven by a `git diff --name-only` acceptance criterion. |
| T-02-04 | Information disclosure | `src/app/(en)/review/page.tsx` reachable on a public URL | low | accept | Gated by an `nnx_` API key with a suspended/role check and returns `notFound()` without one. Unchanged by this plan; only the query text moves. Recorded per research Open Question 1. |
| T-02-SC | Tampering | npm supply chain | high | mitigate | This plan installs nothing. No `npm`, `npx --yes`, `pnpm` or `yarn` install command runs. `package.json` and `package-lock.json` byte-unchanged, asserted by a `git status --porcelain` gate in Task 3. |
| T-02-DB | Tampering | Production Postgres | high | mitigate | `npm run build` is never invoked; it would run `prisma migrate deploy` against the production database. The build gate is `npx next build` only, stated as hard constraint 1 and used verbatim in Task 3's verify. |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0.
2. `npx next build` exits 0. `npm run build` never ran.
3. `node scripts/checks/route-smoke.mjs --verify` against `npx next start` reports 28/28 `ALL PASS`.
4. All seven public call-site files import from `@/sanity/lib/queries` and hold zero inline post GROQ.
5. `src/sanity/lib/queries.ts` holds 12 named exports and zero language-filter text (the filter is plan 02-02).
6. `git diff --name-only` shows no dashboard, `api/dashboard` or cron file, and no change to `package.json` or `package-lock.json`.
</verification>

<success_criteria>
- CONTENT-02's "single shared module" half is satisfied: one module owns every public post query.
- The extraction is a pure no-op: build green, route smoke 28/28, no behavioural change anywhere.
- The four status-predicate variants survive as three named constants plus an explicit no-predicate case, with counts pinned at 2/4/2.
- The pre-extraction git SHA is recorded, so plan 02-02 can prove the relocation was textually faithful.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

| Symbol or path | Kind | Created by | Notes |
|---|---|---|---|
| `src/sanity/lib/queries.ts` | new file | 02-01 | The only home for public post GROQ. Server-only. |
| `STATUS_TOLERANT` | exported const | 02-01 | approved, or status undefined, or due scheduled |
| `STATUS_STRICT` | exported const | 02-01 | approved, or due scheduled |
| `STATUS_APPROVED` | exported const | 02-01 | approved only |
| `blogIndexQuery` | exported const | 02-01 | `(en)/blog/page.tsx`, compound categories + posts + authors |
| `homePageQuery` | exported const | 02-01 | `(en)/page.tsx`, compound hero + latest + categories, 4 filter sites |
| `postBySlugQuery` | exported const | 02-01 | post page body, single-document `[0]` |
| `postStaticParamsQuery` | exported const | 02-01 | post page `generateStaticParams` |
| `postMetadataBySlugQuery` | exported const | 02-01 | post page `generateMetadata`, no status predicate |
| `postsByAuthorSlugQuery` | exported const | 02-01 | `(en)/authors/[slug]/page.tsx` |
| `sitemapQuery` | exported const | 02-01 | `src/app/sitemap.ts`, compound posts + authors, no status predicate |
| `postsByAuthorIdQuery` | exported const | 02-01 | `api/v1/posts` GET, keeps the `"url": select(...)` projection |
| `authorReviewPostsQuery` | exported const | 02-01 | `(en)/review/page.tsx`, no url projection |
| `.planning/phases/02-content-model/artifacts/pre-extraction-ref.txt` | artifact | 02-01 | Git SHA captured before the first extraction edit |
| `EN_LANGUAGE` | exported const | 02-02 | The single English-language predicate (D-03), 12 interpolation sites |
| `scripts/checks/language-filter.check.ts` | new file | 02-02 | Flags: none (static and git fidelity), `--live`, `--post-migration`. Extended by 02-03 and 02-04. |
| `language` | schema field on `postType` | 02-03 | string, list en/fa, radio layout, `initialValue: "en"` |
| `translationOf` | schema field on `postType` | 02-03 | reference to `post`, `options.filter` resolver, `disableNew: true` |
| `translationNotes` | schema field on `postType` | 02-03 | text, `rows: 6`, `readOnly: true` |
| `scripts/migrate-post-language.ts` | new file | 02-04 | Dry run by default, `--execute` to mutate (D-09) |
| Studio list id `posts-en` | structure id | 02-05 | Titled "Posts — English" (D-05) |
| Studio list id `posts-fa` | structure id | 02-05 | Titled "Posts — Farsi" (D-05, D-06) |
</artifacts_this_phase_produces>

<source_coverage_audit>
## Multi-Source Coverage Audit (phase-level, covers all five plans)

**GOAL (ROADMAP Phase 2 success criteria)**

| # | Item | Status | Plan |
|---|------|--------|------|
| 1 | Blog page files render from queries imported from `src/sanity/lib/queries.ts`, no inline GROQ | COVERED (redirect file covered by vacuity, interpretation recorded above) | 02-01 |
| 2 | English listings and post pages return only English documents, filter in exactly one place | COVERED | 02-02 |
| 3 | Every pre-existing post carries `language: "en"` after the one-off migration | COVERED (both datasets) | 02-04 |
| 4 | `postType.ts` carries the three fields and the Studio lists English and Farsi separately | COVERED | 02-03, 02-05 |
| 5 | Ordering invariant: the filter was live before the first Farsi document existed | COVERED (wave order 1 to 5, recorded as a phase invariant with the deploy caveat) | 02-02 |

**REQ (ROADMAP traceability)**

| ID | Status | Plan |
|----|--------|------|
| CONTENT-01 | COVERED | 02-03, 02-04, 02-05 |
| CONTENT-02 | COVERED | 02-01, 02-02 |

**RESEARCH (02-RESEARCH.md features, constraints, pitfalls)**

| Item | Status | Plan |
|------|--------|------|
| 11-query / 8-file inventory, four status predicates preserved | COVERED | 02-01 |
| Single-document queries need the filter as urgently as listings (Pitfall 1) | COVERED | 02-02 |
| Nested `count()` sub-queries on the homepage (Pitfall 2) | COVERED | 02-01, 02-02 |
| `.filter()` replaces the preset type filter (Pitfall 3) | COVERED | 02-05 |
| Studio list id collision (Pitfall 4) | COVERED | 02-05 |
| `initialValue` does not run for `client.create` (Pitfall 5) | COVERED | 02-03 |
| Wrong-dataset migration (Pitfall 6) | COVERED | 02-04 |
| Stale ISR outlives the code change (Pitfall 7) | COVERED as a recorded invariant and handoff note | 02-02 |
| Farsi documents must never carry `status: "scheduled"` (Pitfall 8) | COVERED as a recorded Phase 3 invariant | 02-03 |
| New script must be strict-clean under the tsc gate (Pitfall 9) | COVERED | 02-04 |
| `/review` and `api/v1/posts` must not be merged (Pitfall 10) | COVERED | 02-01 |
| Validation architecture: literal-array fragment test, schema shape import test, listing parity probe | COVERED | 02-02, 02-03, 02-04 |
| Do not import `dotenv`; use `npx tsx --env-file` | COVERED | 02-04 |
| Zero new packages | COVERED | all |
| `sitemap.ts` missing status filter stays unfixed | COVERED as an explicit non-goal, logged for a separate quick task | 02-01 |

**CONTEXT (CONTEXT.md decisions)**

| ID | Status | Plan |
|----|--------|------|
| D-01 filter reach across all public surfaces | COVERED | 02-01, 02-02 |
| D-02 dashboards/admin/cron stay unfiltered | COVERED as an enforced non-goal | 02-01, 02-02 |
| D-03 tolerant filter form | COVERED | 02-02 |
| D-04 both writers stamp `language: "en"` plus `initialValue` | COVERED | 02-03 |
| D-05 two language-filtered Studio lists | COVERED | 02-05 |
| D-06 Farsi preview subtitle shows the English source title | COVERED | 02-03, 02-05 |
| D-07 `translationOf` picker filtered to English posts | COVERED | 02-03 |
| D-08 `translationNotes` read-only | COVERED | 02-03 |
| D-09 migration dry-run default, `--execute`, production gated on Saeid | COVERED | 02-04 |

**Excluded (not gaps):** the deferred ideas in CONTEXT.md (dashboard language filtering, `package-lock.json` repair, `/cv/[slug]` guard) and the milestone-wide exclusion of the 151 visual guides.

**Result: no unplanned items.**
</source_coverage_audit>

<output>
Create `.planning/phases/02-content-model/02-01-SUMMARY.md` when done.

Record in it: the pre-extraction SHA, the final export list of `src/sanity/lib/queries.ts`, the status interpolation counts, the route-smoke result, and an explicit note that the language filter is deliberately absent and lands in plan 02-02.
</output>
